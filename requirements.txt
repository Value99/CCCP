# WINUI-EXE 启动器依赖(Python >= 3.10)
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
PyYAML>=6.0
# 原生桌面窗口外壳(WebView2);缺失时降级为浏览器模式
pywebview>=5.1
# 开发/测试
pytest>=8.0
# 打包(仅构建机需要)
# pyinstaller>=6.0
