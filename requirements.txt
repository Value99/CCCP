# WINUI-EXE 启动器依赖(Python >= 3.10)
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
PyYAML>=6.0
# 模型下载(ModelScope 默认，HuggingFace 备用；两者均随离线环境提供)
modelscope>=1.38
huggingface_hub>=0.24
# 原生桌面窗口外壳(WebView2);启动器不会自动打开外部浏览器
pywebview>=5.1
# 开发/测试
pytest>=8.0
# 打包(仅构建机需要)
# pyinstaller>=6.0
