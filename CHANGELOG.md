# 更新日志

本项目遵循语义化版本；源码变更通过 Git 提交维护，离线发行目录以版本号命名。

## [0.9.3] - 2026-08-16

- 新增清单驱动的通用 Dense VQ 架构适配。Qwen3.5 27B CCCP Dense 模型可由 `cccp.json` 自动识别并复用同一加载、设备选择、上下文、KV 与 OpenAI 兼容 API 链路；没有按模型目录名增加专用分支。
- Dense 模型不显示专家配置选择，也不会进入语料路由扫描；训练页明确说明该架构没有动态专家、无需生成专家配置，首页直接加载完整模型。
- NVIDIA Dense VQ 路径新增原生 FP8 分组投影、静态 KV 缓存、融合递归状态更新与 CUDA Graph；H20 单卡实模验证 Prefill 约 527 token/s、Decode 约 55.56 token/s，输出有限且多轮可读。
- CPU Dense VQ 路径统一使用预生成 Q4 执行映像、码本缓存和 NUMA 感知线程规划；不会把逻辑核心数直接当作最优线程数，也不会混淆 P 核与能效核。
- Kimi 组合 Gate/Up/Down packed 元数据同时支持 10 行与 15 行布局；当 NVIDIA 反量化分组工作区不足或 AMD 不适合私有 grouped-mm 时，改用精确 packed grouped 融合执行器，不回退逐 token/专家 GEMV。
- AMD 全显存 Decode 增加硬性执行器门禁：终端必须显示 `hip.tp1-token-graph` 且 Graph 提交计数增长；检测到旧的 CUDA 名称、参考执行器或未注册的 grouped Prefill 会直接报错，避免用 CPU/兼容路径伪装 GPU 加速。
- 完成 Qwen3.5、DeepSeek-V4、Kimi K3 与 GLM-5.2 实模回归；自动化测试为 313 passed、11 skipped。CPU 性能目标未达到的实测数据记录在独立测试报告中，不作为发行性能承诺。

## [0.9.2] - 2026-08-15

- 修复 DSV4 Decode 热缓存把“上一 token 路由”和语料热专家占满全部槽位后，当前层换入专家覆盖同批已选槽并报 `cannot protect resident packed route slot`：现在按 packed 签名额外预留完整 Top-K 交换窗口，并在单次路由选择期间原子保护所有命中和新上传槽。
- 逐请求缓存审计新增 Decode 实际执行器、融合核提交、CUDA Graph 提交及诊断参考路径计数；正常 CUDA 路径明确显示 `cuda.packed_moe_topk_fused`，不再用 Prefill 搬运计数推测 Decode 是否进入编译算子。
- 双击启动只创建 pywebview/WebView2 原生桌面窗口，不再自动打开系统浏览器；重复启动和原生外壳失败也不会退回浏览器，失败时停止本地后端并显示原生错误。
- GPU 容量预检改为严格的“显存 → 主机内存 → 磁盘”三级降级：显存不足先提示并卸载到内存，只有主机内存也不足时才显示红色磁盘兜底警告；终端进度同步区分 RAM 与磁盘卸载。
- 修复离线 CUDA 首次编译继承系统 `CUDA_HOME`、找不到 pip cuBLAS DLL 或随包 `lib.exe` 的问题；现在固定使用隔离 CUDA SDK，动态发现 cuBLAS DLL，显式定位发行根工具链，并在封装阶段预生成/验证导入库。
- NVIDIA 算子按当前设备自动选择目标架构并缓存，覆盖 RTX 20 系 SM75、RTX 30 系 SM86、RTX 40 系 SM89、H20/H100 SM90 与 RTX 50 系 SM120，不沿用用户终端里的旧架构变量。
- 修复 GPU 算子在每次启动或移动发行目录后重复编译：缓存改为本机应用数据中的跨版本稳定目录，按后端、显卡架构、Torch/CUDA/ROCm 版本和算子源码指纹隔离；命中时直接加载 `.pyd`，不再进入 Ninja/编译器，并在终端明确输出 `cccp-op-cache hit/miss`。
- 关闭原生窗口时会同步停止 CCCP Python 推理子进程并等待本地服务退出；每次重新打开启动器都会清空上一会话终端与聊天性能日志。
- 用户实际点击启动时所选的 CPU/NVIDIA/AMD 后端会写入本机设置，下次打开继续使用该设备。
- 主题选择现在会在切换时立即写入本机设置，并在页面初始化时从后端恢复；不再依赖 WebView 的临时 LocalStorage，也不会因为只切换主题、未点击整页“保存设置”而丢失。
- DSV4 受限显存档改用通用“全配置专家 RAM 驻留 + 有界 VRAM 热槽”：只载入配置白名单中的全部专家，不减少配置专家或 top-k；锁页达到 CUDA/Windows 资源上限时自动退回普通 RAM staging，不再因 `cudaHostRegisterMapped` 耗尽而失败。
- 修复 RAM 驻留专家被误判为“无需预取”的 CUDA 调度错误：固定显存热缓存会先于主机锁页建立，上一 token 的逐层路由在当前 token 计算期间跨层预取，RAM→VRAM 传输与 Attention/共享专家计算重叠；避免 GPU 等待同步 staging、CPU 反而打满。
- Linux/TCC 按实时 RAM 与显卡映射预算自动锁页；Windows/WDDM 不再注册数十 GiB 的碎片专家页，改用固定连续 pinned ring 做层级异步 DMA，避免任务管理器“共享 GPU 内存”上涨，并绕过 RTX 3090 现场约 1.5 GiB/s 的异常碎片直传。显存物理安全线统一提高到 2 GiB。
- 专家加载按约 1% 连续更新，RAM 锁页阶段增加独立进度；不再因配置少于旧 2,000 专家刷新阈值而一直显示 0。
- 聊天 `tok/s` 改为首 token 后的稳态 Decode 口径，不再把 Prefill/TTFT 算入生成速度；终端新增每请求 Decode、缓存命中/未命中、上传量、纯 H2D DMA、主存 staging、直传/中转字节、拷贝次数、显存热槽及锁页 RAM 审计。
- 修复固定地址专家 arena 被通用显存监控器按 0.5 GiB 连续裁小、随后只放宽数字而不恢复物理槽位的问题；该故障会把 20 GiB 显卡的专家热槽永久压到 2.44 GiB，造成 `hit=0` 和每 token 数 GiB 重传。固定 arena 现在由启动期物理上限和单一 2 GiB 安全线定容；启动日志完整输出申请量、Dense 后分配量、驱动可用量、进程上限、单一预留与最终容量。
- 修复启动期仍按 Dense 估算和架构工作区提前扣减专家预算、随后固定 arena 又按实际 CUDA 占用和安全线二次封顶的问题；现在所有架构都把启动可用量交给加载后的实时分配器裁定。普通启动会丢弃父进程残留的旧预留及测试用显存硬上限，只有当前 CLI 显式参数可以覆盖。
- 固定专家 arena 现在真正使用配置中语料扫描得到的 `route_count`：先按最大专家精确预留模型原生 Top-K 动态换入槽，剩余显存按全模型真实命中次数从高到低常驻热专家；不再把训练结果只用于路由白名单、运行时却退化成循环 LRU。无热度配置才使用上一 token 路由保护。
- DSV4 改用 Prefill/Decode 阶段化显存：Prefill 临时只保留能覆盖单层完整紧凑专家的 arena，使扩展专家能够成批执行；完成后自动恢复最大安全 Decode arena。Decode 不再为收益很低的整轮上一 token 路由固定占用 258 个槽，模型原生 Top-K 工作槽之外的全部空间用于语料热专家；热专家保护到显存后释放其 RAM 重复载荷，下一次完整 Prefill 会先带进度恢复紧凑主机源再重分区。2 GiB 是统一物理安全线，不会被阶段工作区重复扣减。
- 新增 `[cccp-residency-plan]` 容量账：逐项输出 Dense/固定运行区、统一安全余量、Prefill 单层最大工作集、码本、最大专家、模型 Top-K 字节底线、签名槽实际需求、Decode arena、热专家数与可去重主机体积。
- 修复显存热专家 RAM 去重在 Windows 错误调用 POSIX `malloc_trim` 的 `TypeError`；清理主机副本前先释放批量 `DeviceExpert` 临时视图，异常 traceback 不再持有旧 arena 并使下一次 Prefill 容量误算为零。
- 终端新增 `[cccp-cuda-audit]` 启动回执，明确显示 Dense 实际驻留、专家 CUDA 算子、RAM 专家体积、显存热缓存及跨层预取状态。
- CUDA 13 运行时按当前隔离环境动态查找 `cudart64_*.dll`，不再硬编码 CUDA 12；终端按行识别 UTF-8 与 Windows 编译器代码页，修复中文路径下编译输出乱码。
- 保留 GLM 前置 Dense 层修复：严格 top-k 仅检查 `cccp.json` 声明的专家层 L3–L77。
- Kimi K3 已接入同一套清单驱动训练、CPU 与 CUDA 路径：真实 H20 路由扫描生成 11,315 专家/55.43 GiB 配置；CPU 严格配置、全显存 CUDA 和 16 GiB RAM-Dense CUDA 均完成实际中文生成，没有减少专家或改变 top-k=16。
- Kimi 受限显存 Prefill 改为整块 host/GPU 传输和分层批处理；修复 KDA/MLA 状态设备不一致、反量化工作区碎片 OOM，以及 Prefill 临时专家 arena 未在 Decode 前恢复的问题。显存不足以容纳 Dense 时自动使用 RAM Dense + CUDA packed MoE，并明确显示兼容降速提示。
- Kimi 全显存 H20 实测 4,025-token Prefill 约 96.3 token/s，短请求 Decode 约 5.7 token/s；16 GiB 兼容档保留完整配置专家并成功输出，但 22-token Prefill 约 116.3 秒，因此不标记为高速档。
- DSV4 16 GiB 档真实保留 11,008 个配置专家并全部驻留 RAM，44 GiB 锁页、1.54 GiB / 258 个显存热槽，运行期零磁盘；修复 Prefill 按层分区后下一层需要更宽 arena 时未恢复全局布局的问题。622-token Prefill 实测约 135 token/s，中文生成、EOS、思考档位与多轮 KV LCP 重放通过。
- 修复 DSV4 CUDA 分层 Prefill 的专家 H2D 竞态：当前批专家在复制流写入完成前不再被默认计算流提前解量化；Windows/WDDM 另行禁用 CUDA 13 原生 `cudaMemcpyBatchAsync`，改用同一 copy stream 的多次非阻塞 DMA，保留整层批处理与计算重叠，避免异步 `cudaErrorIllegalAddress` 延后冒泡到目录更新。Linux/H20 继续使用原生批量 API。
- GLM 多轮聊天支持在聊天模板边界发生重分词时截断到最长公共前缀并继续复用 KV；修复 CPU 旧 VQ 路径逐 token/专家投影，改为按专家聚合完整 token 行并调用融合列表算子，11-token CPU Prefill 由 104.229 秒降至 53.671 秒。
- GLM 的角色 EOS 不再泄露到可见回答，流式思考解析支持跨分块 `</think>` 并把推理与正式回答分离；H20 全 19,456 专家 CUDA 实测可读中文、EOS、KV 精确前缀复用均通过。811 专家窄配置在 CPU/CUDA 均出现重复输出，已确认是配置覆盖不足，不能当作通用质量预设。
- 离线冒烟会向整个冻结进程树设置 `PYTHONDONTWRITEBYTECODE=1`，避免依赖探测在 SHA-256 清单生成后向发行目录写入未登记的 `__pycache__`；清理并重建清单后，真实冻结 API 冒烟保持 0 个新增缓存目录。
- 生成 ZIP64、分卷和安装器前新增不可跳过的发行清单门禁：逐文件验证路径安全、文件集合、总字节数和 SHA-256，任一缺失、多余、篡改或清单路径越界都会停止发布。
- 完整自动化回归为 270 passed、11 skipped；CUDA 13 工具链枚举包含 SM75/86/89/90/120，SM75、SM86 与 SM120 融合算子均已在无显卡强制模式下完成真实 `nvcc + link.exe + cuBLAS` 编译链接。SM86 Windows 安全层批量 DMA 新源码首次完整编译成功、第二进程命中缓存并导出 `packed_h2d_batch` 与融合 MoE 接口。

## [0.9.1] - 2026-08-14

- 完成 333.524 GiB GLM‑5.2 CCCP 模型的通用配置驱动识别、157/157 制品 SHA‑256 校验、4096-token 路由扫描、专家热力图、32.296 GiB 配置保存、加载与真实 GUI 流式生成回归。
- 修复严格路由校验把 Dense 层 0–2 当作专家层，错误提示“少于模型 top-k=8”的问题；新配置精确记录专家层，严格 top-k 只检查模型声明的 MoE 层。
- 修复组合 Gate+Up/Down Q4 专家在单 token 解码时误入旧 packed 索引算子而触发 payload length mismatch；现在按投影布局选择通用融合执行器，不按模型名增加专用分支。
- 正常聊天复用分组批量 Prefill、CPU 码本缓存和 Q4 执行映像；流式错误会写入终端并明确通知前端，不再以残缺 SSE 假装完成。
- 修复达到 `max_tokens` 后仍多执行一次完整模型 decode；GLM 的最终未提交 token 会在下一轮安全重放，1-token 真实 GUI 回归由 469.7 秒降至 268.3 秒。
- 聊天页显示实际运行实例的配置，性能指标按请求 ID 精确关联；只有收到 `[DONE]` 才把生成视为完成。
- 本版完整自动化回归为 147 passed、11 skipped；跳过项均为当前机器缺少 NVIDIA/AMD 硬件的设备测试。

## [0.9.0] - 2026-08-13

- 增加非阻塞双来源更新检测：VisionSic 官网优先、GitHub 兜底，支持稍后提醒和持久忽略当前远端版本；同为 0.9.0 时不提示升级。
- 修复 AMD/CUDA 自动上下文路径未导入后端探测函数的问题，并补齐 GPU 路径回归测试。
- 增加上传大小/频率限制、跨站写入保护、下载目录边界和持久化原子写入。
- 完成 CCCP I‑1～I‑5 集成契约：静态/HTTP 模型规格、逐专家精确字节表、路由统计启停、mapped 磁盘档位和加载驻留回执。
- 训练协议改为累计 token：默认/最小 500,000 token，移除样本条数和目标 GiB 两套旧字段。
- JSONL 完整保留 system/user/assistant 角色，使用模型聊天模板做 teacher-forcing 纯 prefill，不生成回答。
- CCCP Engine 新增 `route-scan`，固定 4096-token prefill 分块并向启动器回传实时 token 进度。
- 路由扫描新增分词/加载/prefill 分段计时、token/s、10 秒运行心跳和机器可读 CPU 算子审计。
- Windows 自动探测实际 L2/LLC 拓扑；块内专家异步预取与共享分支重叠，真实 64-token A/B 提升约 7.5%，路由逐项一致。
- 扫描完成显示专家热力图；用户按逐层路由覆盖率选择专家，界面实时显示专家数与预计容量。
- 配置名称和介绍移动到扫描结果阶段，保存/导出时写入模型身份、token、覆盖率和容量元数据。
- 删除训练失败时的启发式伪路由回退；语料 token 不足会明确失败。
- 通过 118 GiB DSV4 异构 p13/p14 模型全量 11,008 专家、聊天、SSE、KV、惩罚参数与路由扫描回归。
- JSON API 显式声明 UTF-8，修复 PowerShell 5.1 中文模型路径往返；隐藏 DSV4 EOS 并避免热会话台账重复 EOS。

## [0.8.0] - 2026-08-13

### 新增

- 持久语料库：JSONL/TXT 格式说明、有效样本/无效行/大小/路径信息、勾选复用与删除。
- 真实 CCCP 小样本逐层路由扫描，训练产物卡可查看专家、保存、导出和删除。
- 聊天思考强度、折叠思考过程、回退一轮，以及 KV 续接、prefill、TTFT、tok/s 指标。
- 离线发行 CLI 与代码更新/交接规范。

### 变更

- 模型预设跟随精确模型存放到 `<模型>/profiles`；分享导入配置按模型名和 `cccp.json` SHA-256 自动匹配。发行包不带模型和特定模型预设。
- 训练加载策略简化为“自动高速（容量不足逐级降级）”与“强制硬盘”，两种模式都完整保留专家。
- 窗口先隐藏到 WebView 首个样式化页面加载完成，消除启动白闪。

### 修复

- 修复训练三栏、语料选项卡与长配置卡徽章/标题错位。
- 修复聊天区多余闭合标签造成训练及后续页面脱离主内容区；训练三卡改为自适应三等分且等高。
- 首页三个启动操作仅统一高度与基线，宽度随文字自动撑开；性能指标移到对应 Assistant 气泡下方。
- 动态列表增加统一空状态占位；设置页控件后说明文字统一保留 10px 间距。
- 修复中止生成后前端历史未保留部分回答，导致下一轮无法复用已提交 KV 的问题。

## [0.7.7] - 2026-08-12

### 新增

- CPU、NVIDIA CUDA、AMD ROCm/HIP 三套互相隔离的本地推理环境与前端环境选择。
- ModelScope 默认下载源，以及重复惩罚、存在惩罚生成参数。
- CUDA/AMD 融合算子离线预编译与首次启动自动编译路径。
- `VERSION`、SHA-256 清单、版本化离线封装脚本和完整中文交付文档。

### 变更

- 推理引擎、Python 包、协议字段、环境变量、日志和构建产物统一命名为 CCCP Engine / CCCP Launcher。
- 明确配置预算只约束专家配置生成；加载前按共享专家、dense 与去重后的专家集合估算实际 RAM/VRAM。
- 首页、按钮、标题栏、导航激活态、训练切换按钮和聊天布局统一为暖红暗金主题。

### 修复

- 修复亮色模式标题栏仍为深色、Banner 文字遮罩与右侧透明、按钮底部裁切和边距不一致。
- 修复 24 GiB 配置预算被错误当作运行内存风险阈值、加载进度与终端面板信息不足。
