# CCCP Launcher 0.9.3 测试报告

测试日期：2026-08-17
测试分支：`codex/qwen35-cpu-opt-v093`

## 1. 结论

- 通用 Qwen3.5 Dense VQ、架构配置驱动的 MTP、GUI 闭环、NVIDIA 单卡实模推理与原有 DeepSeek-V4、Kimi K3、GLM-5.2 回归通过。
- H20 单卡 Qwen3.5 的 MTP Top-3 验证接受 49/57 个草稿 token，有效 Decode 93.209 token/s；包含 Prefill、草稿和验证的完整 64-token 请求为 82.392 token/s。
- CPU 功能和数值正确，但性能目标未达到：本机约 1.5 token/s；H20 双路 Xeon 最优 Decode 约 7.29 token/s。因此“本机尽量大于 10、H20 CPU 大于 30”不能标记为通过。
- AMD 的当前源码强制使用真实 HIP TokenGraph；旧测试包日志中出现的 `cuda.packed_moe_topk_fused` 与 2.11 token/s 不是当前路径。本轮没有可用 AMD 真机，因此只通过离线依赖、编译路由与自动化门禁，不声明 AMD 实模性能通过。

## 2. 测试对象

| 模型 | 架构 | 测试设备 |
|---|---|---|
| qwen3.5-27b-cccp-15g-sweet | Dense VQ | 本机 CPU、H20 CPU、H20 单卡 |
| dsv4-cccp-s-noblack-v2 | DeepSeek-V4 MoE | H20 单卡 |
| Kimi-k3-cccp-470 | Kimi K3 MoE | H20 四卡 |
| glm-cccp-int4dense-vv-route32 | GLM MoE | H20 双卡 |

## 3. 自动化回归

- Pytest：322 passed、11 skipped、0 failed。
- 跳过项为当前测试节点缺少对应物理设备或只在发行机执行的环境测试。
- Kimi 旧组合投影 `[10,E]` 融合算子数值测试：最大绝对误差 `5.820766e-11`，平均绝对误差 `4.50078e-12`，全部为有限值。
- 同时覆盖 Kimi 15 行元数据、NVIDIA 工作区不足时的 packed grouped 选择、HIP 强制 packed grouped、Dense 训练禁用、架构清单识别、OpenAI API、更新检测与发行审计。

## 4. Qwen3.5 Dense VQ

### 4.1 H20 单卡

| 项目 | 结果 |
|---|---:|
| 模型加载 | 11.66 s |
| 显存 | 26.828 GiB |
| 256-token Prefill | 527.095 token/s |
| MTP Top-3 有效 Decode | 93.209 token/s |
| 64-token 完整请求 | 82.392 token/s |
| MTP 草稿接受 | 49/57 token |
| 数值 | logits 全部有限 |
| 多轮 | 实际两轮可读输出通过 |

执行路径使用原生 FP8 分组投影、静态 KV、融合递归状态、CUDA Graph 和架构配置驱动的 MTP；没有进入逐 token/专家 GEMV。关闭投机后的贪心基线为 24.451 token/s；MTP 输出与贪心输出不要求逐 token 完全一致，因为 Top-3 接受本身允许选择主模型前三候选。

### 4.2 CPU

H20 节点为 2× Intel Xeon Platinum 8558P，96 个物理核心、192 个逻辑线程、2 个 NUMA 节点。线程扫描结果：

| 线程 | Prefill token/s | Decode token/s |
|---:|---:|---:|
| 24 | 9.269 | 5.411 |
| 48 | 15.293 | 6.585 |
| 72 | 18.808 | **7.290** |
| 96 | 20.251 | 6.433 |

Q4 NUMA-local 行分片在 96 物理核心下为 Prefill 27.641 token/s、Decode 6.961 token/s。结果说明线程并非越多越快；自动策略不能简单使用逻辑核心总数。本机真实 GUI CPU 生成约 1.5 token/s。CPU 路径功能通过，速度目标失败。

## 5. 原有模型实模回归

### DeepSeek-V4

- H20 单卡，11,008 个配置专家，68.72 GiB，全显存，RAM 专家 0，运行期专家 H2D 0。
- 加载 163.6 s，有限 logits；8 个强制 Decode token 为 31.905 token/s，实际中文输出以“一等于二。”开头，退出码 0。

### Kimi K3

- H20 四卡 TP4，82,432 个专家，414.23 GiB，全显存，RAM 专家 0，运行期专家 H2D 0。
- 1–92 层全部显示 `executor=cuda.packed-moe-grouped-fused`。
- 大规模固定图生命周期改为“专家数据先固定、父图完成后最终捕获全部 KDA”；Kimi TP4 暂停不稳定的整层 Decode 父计划，保留 KDA/MLA、Dense、shared/router 与 packed MoE 融合子图。
- 最终回归加载 284.7 s；30-token Prefill 7,114.0 ms；完整走完 93 层 Decode，实际输出严格为 `2`，退出码 0，无非法显存访问。

### GLM-5.2

- H20 双卡 TP2，19,200 个专家，189.84 GiB，全显存，RAM 专家 0，运行期专家 H2D 0。
- 加载 450.8 s；“一加一”实测输出严格为 `2`，退出码 0。

## 6. GUI 回归

- 从 GUI 添加并识别 Qwen3.5 Dense 模型，不依赖模型目录名。
- 首页 Dense 模型不要求选择专家配置，可直接预检和启动完整模型。
- 训练页对 Dense 明确显示无需训练，不出现专家扫描、热力图或保存专家配置入口。
- 真实 CPU 启动、自动切换终端、加载进度、OpenAI SSE 聊天、停止、聊天气泡性能指标均通过。
- 关闭桌面应用时后端终止；设备与主题保持记忆；重开后终端为新会话。

## 7. AMD 验收门禁

当前源码在 AMD 全显存 MoE Decode 时必须满足：

1. 终端执行器为 `hip.tp1-token-graph`；
2. 请求统计 `decode_graph > 0`；
3. 不得出现 `cuda.packed_moe_topk_fused`、参考执行器或逐专家 GEMV；
4. Prefill 使用注册的 packed grouped 执行器。

此前 2.11 token/s 的日志来自旧 r27 文件组合，不能代表当前实现。0.9.3 完整包通过全目录清单与 SHA-256 防止零散覆盖造成版本混用；AMD 未经本轮真机复测的限制会保留在发布说明中。

## 8. 发行门禁

发行包不得包含模型、测试语料、量化/训练框架源码、开发文档、测试报告、Pytest 文件或本地缓存。只允许包含冻结启动器、三套离线推理环境、推理运行时、离线编译工具和三份用户基础文档。本轮已获得用户明确的合并、测试和发布授权。

## 9. DSV4 长上下文 FlashMLA 严格门禁

- 删除 DSV4 `topk512` 路径中的 BF16 Indexer、`torch.topk` 与 BF16 全量 Attention 静默回退；FP8 Indexer、固定工作区 Top-K、FlashMLA SplitKV 或逆 RoPE 任一步不可用都会直接报错。
- 删除 `CCCP_SPARSE_SPLITKV` 选择开关。NVIDIA CUDA 最大上下文进入 2052 token 以上时必须具备 FlashMLA；2051 token 以内继续使用固定 direct bucket，不属于回退。
- H20 不提供 FlashMLA 包时，初始化按预期失败并明确报告 `禁止退回 sparse=bf16-exact`。
- H20 提供 FlashMLA 后，4096-token 上下文完整实模通过；TokenGraph 日志为 `sparse=flashmla-model1-fp8-splitkv`，未出现成功态 `bf16-exact`，64-token Decode logits 全部有限。首个 32-token 图捕获/热身窗口为 13.03 token/s，随后稳态窗口为 32.36 token/s。
- 修复用户上下文导致的 FP8 Indexer 矩阵宽度不对齐：4368 最大上下文的 1092 个逻辑候选使用 1104 宽执行映像，逻辑缓存范围保持不变。
- Windows CPU 环境 `tests/test_runtime.py`：133 passed；新增严格门禁与对齐用例通过。
- 当前 Windows CUDA 环境尚未内置 FlashMLA；因此 NVIDIA DSV4 在 2052 token 以上会明确报缺失，而不会伪装成可运行的慢速 BF16 路径。0.9.3 的默认可用范围按 2051 token 以内发布；长上下文支持不作承诺。

## 10. 最终封装与公开发布

- 最终 EXE：40,780,911 字节；SHA-256 `583D1E627B6F2479AE60BF2E5260982B18D94924F1BDD619DE4CE9FCC446E5E1`。
- 离线目录：105,363 个清单文件、15,381,473,644 字节；逐文件复算结果为 0 缺失、0 多余、0 不匹配。
- 离线归档：6,505,133,446 字节；4 个分卷顺序重组后的 SHA-256 为 `7457c7ead445e05afbcfce3bd3780a4a4690d1d8be661383bf4ab5b70d8ccdd6`。
- 双击安装器真实运行退出码 0，完成分卷校验、重组、解压、临时 ZIP 删除和应用自动启动；安装后 EXE 哈希与版本再次核对一致。
- GitHub 公共仓库 `main` 为 `8751c95`；`v0.9.3` Release 共 9 个资产，公开 API 返回全部 `uploaded`，字节数和远端 SHA-256 digest 与本地一致。
- VisionSic 本地站点文件已更新为 0.9.3，但公网 IIS 仍返回 0.9.2。由于本机未配置该 IIS 的发布凭据或控制面板会话，本报告不把官网写成已上线；GitHub Raw 更新清单已公开为 0.9.3。
