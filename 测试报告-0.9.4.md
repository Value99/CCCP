# CCCP Launcher 0.9.4 测试报告

日期：2026-08-17

## 结论

- Linux 双路 CPU Q4 NUMA 页归属问题已修复；真实 Qwen3.5 27B Dense VQ 64-token Decode 为 9.772 token/s，输出有限。
- 同机器、同模型、同 96 物理线程的旧稳定结果约为 7.05 token/s，本补丁提升约 38.6%。
- 设想的 H20 CPU 30 token/s 仍未达到，因此该指标不标记为通过，也不写入发行性能承诺。
- Windows、NVIDIA、AMD、MoE、MTP、GUI 与配置格式没有改变；自动化完整回归通过。

## 实测环境与结果

- 模型：`qwen3.5-27b-cccp-15g-sweet`，本地与 H20 节点的 `cccp.json` SHA-256 一致。
- CPU：双路 Intel Xeon Platinum 8558P，96 物理核 / 192 逻辑线程，2 个 NUMA 节点。
- 执行映像：CPU Q4，11.94 GiB；MTP 在 CPU 默认关闭。

| 物理线程 | Prefill token/s | Decode token/s | 说明 |
|---:|---:|---:|---|
| 48 | 14.150 | 6.720 | NUMA 分片启用 |
| 72 | 15.538 | 8.207 | NUMA 分片启用 |
| 96 | 18.747 | 8.460 | 16-token 扫描 |
| 96 | 16.708 | **9.772** | 64-token 稳定扫描 |

64-token 扫描的 11,456 次常驻投影累计 1.721 秒，4,032 次 Q4 SwiGLU 的 Gate/Up、Down、规约累计 2.304 秒；最终 logits 全部有限。

## 修复验证

- 迁移前：Q4 输出张量可能复用已触页的分配器内存，`mbind` 只设置后续策略，既有页仍集中在错误 NUMA 节点。
- 迁移后：两半连续输出行先尝试 `MPOL_MF_MOVE` 迁移到节点 0/1；权限或内核不支持时退回普通 `mbind`，不影响功能。
- 自动线程只在 Linux + Q4 + NUMA 分片 + 大型 SMT 服务器启用全部物理核；逻辑线程和 Windows 能效核不会被误当作等价核心。

## 自动化回归

- 完整测试：**324 passed、11 skipped、0 failed**。
- 原生算子重点子集：40 passed、3 skipped。
- Windows v195 CPU 算子本地编译、预编译加载和 Q4/Attention/融合 MLP 数值测试通过。

## 发行边界

- 离线包不含模型、模型预设、测试报告、启动器工程源码或 CCCP 量化/训练框架。
- CPU、NVIDIA CUDA、AMD ROCm/HIP 三套环境和离线编译工具链随包提供。
- 本补丁不重复声明未重新测量的 AMD 性能；NVIDIA 与四类模型路径沿用 0.9.3 已发布回归结果。

## 发行成品验证

- `CCCP-Launcher.exe`：40,781,747 字节，SHA-256 `75CF6EB189F861BEBE2A529F0D35F34C68DBA0CC88791E65E1886D6FEE778E37`。
- 离线目录：105,363 个清单文件，共 15,381,475,504 字节；逐文件复算结果为 0 缺失、0 多余、0 哈希不符。
- 冻结 EXE 从离线目录启动成功，0.9.4 版本、CCCP 引擎状态和 7 个核心 API 端点通过，测试结束后进程与端口正常回收。
- ZIP64 归档：6,505,134,338 字节，SHA-256 `00B68247A015246622D21453AC2C500305DF75A227BF26DD56688642B555B42B`；拆分为 4 个 GitHub 分卷并逐个复核 SHA-256。
- 首次安装器：184,320 字节，SHA-256 `421C0CCABDD80C574AE508947F465C33BDEB129107BC02F1D122404987580672`。

## 公网发布复验

- 公开仓库 `main`：`f6cce415efa896680706031265fc331a766e21d1`；主页、三语言下载说明、`VERSION`、`latest.json` 与独立 EXE 均为 0.9.4。
- GitHub Release：`https://github.com/Value99/CCCP/releases/tag/v0.9.4`，非草稿、非预发布，共 9 个手工附件；全部状态为 `uploaded`，远端字节数和 GitHub `sha256:` digest 与本地一致。
- GitHub Raw `latest.json` 公网直读为 0.9.4，下载地址、EXE 字节数与 SHA-256 均一致。
- VisionSic 公网 `/cccp/latest.json` 仍为 0.9.2；本地官网成品已更新到 0.9.4，但当前机器没有该 IIS 的 FTP、WebDeploy、SSH 或控制面板发布凭据，因此未把官网写成已上线。
