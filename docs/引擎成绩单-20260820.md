# CCCP 推理引擎成绩单（2026-08-20，H20-3e 139.8GiB / 192 核 / 2TB RAM）

全部实测零报错；decode 为扣除 prefill 后的稳态速率，wall 为含 prefill 的端到端速率。

## CUDA（单卡）

| 模型 | 配置 | prefill | decode | wall | 显存 |
|---|---|---|---|---|---|
| qwen3.8-27B int4 | plain（默认） | 28.4 tok/s | **46.15 tok/s** | — | 15.90GiB |
| qwen3.8-27B int4 | MTP v30（VERIFY=q8, draft 4） | — | **56.98–66.51 tok/s**（运行方差带，标称 66.51） | — | 15.65GiB |
| DSV4（dsv4-cccp-s-noblack-v2） | 快档全链（run_dsv4_fast.sh） | 11.8 tok/s | **30.02 tok/s** | 25.25 | ~93GiB（专家全驻留 68.7GiB） |
| GLM int4dense | 满卡 cap=130 | **106.0 tok/s** | **8.12 tok/s** | 8.02 | ~129GiB（专家槽 115.3GiB） |
| GLM fp8dense | 单卡 | 2.4 tok/s（冷启动首填 8.4s） | 5.28 tok/s | 3.61 | ~137GiB 满卡 |
| Kimi K3-470B | 单卡（14% 专家驻留） | 1.7 tok/s | 3.42 tok/s | 1.04 | ~130GiB |

## CPU（192 核，2TB RAM）

| 模型 | prefill | decode | 备注 |
|---|---|---|---|
| qwen3.8-27B | 17.5 tok/s | **7.06 tok/s** | 本轮修复三缺陷后首次可跑 |
| DSV4 | 2.5 tok/s | 8.70 tok/s | |
| GLM int4dense | 8.3 tok/s | 0.46 tok/s | 192GB 专家内存池 |
| Kimi K3-470B | 0.1 tok/s | 1.34 tok/s | 470B 纯 CPU |

## 质量门禁

- qwen 聊天验证（MTP）：接受率 5/12 + 2/12 精确基线保持；finite_logits=true。
- 全部 8 组运行（CUDA 6 + CPU 4，含 qwen 回归三件套）零报错。

## 本轮（第三十七轮）修复的既有缺陷

1. qwen CPU：DenseVQEmbedding 的 uint16 行索引不被 torch 高级索引接受 → 转 int64。
2. qwen CPU：fla chunk 入口（实例级绑定）无设备检查，CPU 张量直入 Triton → 实例级设备分派包装。
3. qwen CPU：fla FusedRMSNormGated（Triton-only 门控范数）→ CPU 实例换建模层自带 torch 等价实现（权重同源拷贝）。

## 关键配置

- qwen MTP：`CCCP_DENSE_VQ_GPU_IMAGE=int4 CCCP_INT4_GEMV_VERIFY=q8`（v30 Q8×Q4 dp4a）
- DSV4：`scripts/run_dsv4_fast.sh`（PACKED_FULL_GPU+TP_HIDDEN+LAYER_GRAPH+STATIC 图+DENSE_BF16=all+GPU_FP8_EXECUTION）
- GLM：`CCCP_VRAM_LIMIT_GB=130`（满卡，专家槽 115GiB）
