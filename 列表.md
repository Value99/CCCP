# CCCP-Launcher 完善与离线交付清单

最后更新：2026-08-17（v0.9.4 CPU NUMA 补丁正在封装发布）。`[x]` 已完成并验证；`[-]` 已实现但硬指标未达到或受明确条件限制。

## 一、方向与不可违反项

- [x] 只生成专家配置文件，不生成、裁剪或改写模型权重。
- [x] 删除并归档所有“减少专家/top-k=4”实验；始终保留各模型原生 top-k（DeepSeek 为 6，当前 GLM 为 8）。
- [x] 24 GiB 定义为配置生成预算：先扣 Dense 和共享专家，再计算动态专家槽位。
- [x] 运行内存独立估算；当前 24 GiB 配置执行估算约 28.61 GiB，30 GiB 配置仍可能对应 32–34 GiB 或更高实际峰值。
- [x] 红色风险已区分：配置超过设备容量时提示 offload/分页；配置可装下但当前空闲内存不足时提示关闭占内存程序。不自动删专家。
- [x] 前端模型和领域信息只从模型目录、模型随附 `profiles` 与用户导入配置目录读取；发行包不带特定模型预设。

## 二、模型与配置身份

- [x] 模型发现并校验名称、版本、架构、格式版本、完整字节数/GiB、层数、专家数、top-k 和指纹。
- [x] 配置记录模型身份、Dense/共享/固定体积、配置预算、配置权重体积。
- [x] 每个动态专家记录 `层号:专家号`、精确大小、路由命中数和分数。
- [x] 严格校验重复键、专家边界、字节/GiB 换算、固定权重拆分和每层 top-k 覆盖。
- [x] 不同模型版本/格式/指纹的配置禁止组合。
- [x] 导入、导出、分享、同 ID 覆盖、所有配置可删除；配置记录模型名/版本/格式和完整指纹并自动匹配模型。
- [x] 模型库可删除当前扫描到的 CCCP 模型目录；运行中模型与任意外部路径禁止删除。

## 三、多配置去重

- [x] 多配置按模型指纹 + 专家编号求并集，重复专家只计算一次。
- [x] Dense 与共享专家固定权重只计算一次。
- [x] 前端显示唯一专家、动态体积、固定体积、重复专家节省、固定权重复用和总去重节省。
- [x] 爱情 + 沉浸实测：3394 唯一专家、29.053 GiB 配置体积、18.942 GiB 总去重节省。

## 四、语料与新配置

- [x] JSONL 完整保留 system/user/assistant 角色与超长上下文；TXT/纯文本字段按单条 user 消息处理。
- [x] 扫描阈值为累计 token，正式任务默认 500,000；流程验证可使用较小的 4096 倍数，训练界面只保留这一种扫描口径。
- [x] 使用模型聊天模板执行 teacher-forcing 纯 prefill；已有 assistant 回答参与路由统计，不生成新回答。
- [x] DSV4 长上下文固定 4096 token 外层块，并向启动器实时回传已处理 token 进度。
- [x] 训练详情记录分词/模型加载/prefill 秒数、真实 token/s 与 CPU 算子审计；长块每 10 秒回传运行心跳，避免假死观感。
- [x] 使用 CCCP CPU 真实路由统计；扫描失败或语料 token 不足时明确失败，不产生启发式伪配置。
- [x] 扫描结束生成专家热力图；逐层按用户覆盖率选择最小热专家集合，并显示精确容量。
- [x] 配置保存记录模型身份、专家编号/体积、token 总量、分块大小、目标/实际覆盖率和总驻留体积。
- [x] 真实模型 route-scan 冒烟：64 token、16,512 次路由观察、2,285 个非零专家、generation_tokens=0。
- [x] 生成爱情角色扮演配置：2623 专家，23.998180 GiB，覆盖率 95.6983%。
- [x] 生成沉浸对话配置：2509 专家，23.996150 GiB，覆盖率 97.7259%。
- [x] 生成世界观叙事配置：2508 专家，23.996025 GiB，覆盖率 97.2661%。
- [x] 每层最少 26–28 个专家，大于 top-k=6；没有把专家数压到几百个。

## 五、启动器功能

- [x] 设置读写/迁移、路径规范化、CPU 默认、32 GiB 风险阈值和 Q4 默认。
- [x] 自动发现模型、CCCP 引擎和项目内 Python。
- [x] CPU、NVIDIA CUDA、AMD ROCm/HIP 三套 Python/PyTorch 环境隔离；前端按真实自检结果启用设备。
- [x] CUDA/HIP 融合算子使用随包编译工具链首次自动编译；无 GPU 构建机上分别为 `sm_120` 与 `gfx1151` 完成整扩展编译、链接和 Python 加载；失败硬门禁并进入终端日志。
- [x] 模型完整性、端口、内存、配置兼容性、CPU/GPU 和算子预检。
- [x] 启动、就绪轮询、停止、重复启动保护、退出回收和日志查看。
- [x] dry-run 预览使用参数数组，支持中文/空格路径。
- [x] 聊天流式/非流式代理、停止、会话增删改查和 OpenAI 兼容入口已由自动化测试覆盖。
- [x] 训练任务、语料管理、进度、scores/counts/profile 导出和注册已由自动化测试覆盖。
- [x] 训练前不再用体积预算裁剪专家；覆盖率选择后按精确专家大小计算动态、固定与总驻留体积。
- [x] 配置生成时可自定义名称；配置库可编辑名称与说明，同时保持专家编号和模型身份不变。
- [x] 配置卡、首页和组合汇总统一显示配置总驻留（Dense + 共享专家 + 已选动态专家）；实际运行预检另加 KV、工作区与程序开销，口径不再混用。
- [x] 社区配置下载未开放前隐藏；配置删除改为对齐的垃圾桶图标。
- [x] 训练页按“token 扫描 → 热力图/覆盖率 → 名称和介绍 → 保存配置”闭环，旧输入与文案全部删除。
- [x] 下载/社区输入校验、离线快速失败和本地功能不依赖网络。
- [x] 模型下载默认 ModelScope；ModelScope 1.38.1 与 HuggingFace Hub 1.24.0 已内置，无需用户安装。
- [x] 聊天 UI、OpenAI 请求与 CCCP 采样已接通重复惩罚、存在惩罚；启用惩罚时不会走忽略参数的投机路径。
- [x] 首页默认 Discord 社区与 ValueFX ModelScope 模型主页入口，均通过设置加载。
- [x] 单实例互斥量和 UI/CCCP 端口占用保护。

## 六、前端与桌面

- [x] 首页、配置库、模型库、训练、聊天、终端、API、设置页面均加载。
- [x] 首页模型摘要在异步目录扫描后同步刷新，不再残留“未发现模型”。
- [x] 首页模型摘要只显示当前模型，不再重复显示容易受异步刷新影响的“配置数量”；实际配置选择以配置卡为准。
- [x] 配置库只显示当前目录三份实测配置；旧领域示例不进入前端。
- [x] 实际页面显示模型版本、完整模型体积、指纹、专家和去重体积。
- [x] danger 预检实际显示红色样式、offload 警告和“不建议加载”。
- [x] 启动后自动进入终端；单一终端窗口实装结构化加载进度、阶段、专家计数、PID、服务模型 ID、内存摘要和合并日志。
- [x] 内存/显存不足时，用户确认后自动切换 mapped + 系统虚拟内存/磁盘卸载，终端明确提示降速且不删专家。
- [x] 聊天代理不再把本地目录作为模型 ID；错误模型路径会规范成服务模型 `winui-model`，流式聊天实测不再出现 `model_not_found`。
- [x] 聊天栏无横向溢出；Assistant 性能指标紧贴对应气泡下方，左栏不再重复占用空间；首页“一键启动/预检/停止”宽度按内容自适应且高度、基线一致。
- [x] 训练页面 DOM 层级修复；三张卡使用三等分自适应宽度、严格等高并保持正常纵向页面滚动。
- [x] 训练任务运行中只显示中间热力图与进度；覆盖率、名称/介绍和“保存配置”仅在扫描完成且已有专家规划后出现，避免无结果按钮误导。
- [x] 专家热力图按层独立归一化，避免均匀层因单格命中较低而整体发暗；覆盖率逐层独立计算，95% 均匀层自动化门禁验证为 244/256 专家，而集中层可为 top-k=6。
- [x] 本地模型、配置、首页快捷配置、下载任务、语料、训练产物和历史会话均有统一空状态，不再显示疑似加载失败的空白区域。
- [x] 首页保留原单层布局；Banner 只作为原区域背景，不移动操作区或下方卡片；仅在左侧文字覆盖区使用深红渐变遮罩，并在中部淡出，不影响右侧人物和标志。
- [x] 红色与米金按钮按参考图使用低对比渐变、细高光、2px 外部底沿和轻投影；按钮预留底沿间距，训练页模式切换同步该样式。
- [x] Sidebar 升为最高外壳层级并从窗口顶端开始，Logo 位于左上角；标题栏只覆盖右侧主内容区，不再在左侧产生空白条或重复标题。
- [x] Sidebar 激活导航使用更亮的实色暖红底、浅金白图标/文字、加粗字重与细高光边，深色主题下保持可读。
- [x] 目录内 `banner.jpg`、透明 `icon.png` 与多尺寸 `icon.ico` 已接入 WebUI 和 EXE；透明图标保留原始主体像素，仅移除四角连通黑底。
- [x] pywebview 原生窗口启动；缺少 WebView2 时显示原生错误并关闭本地服务，不调用系统浏览器。
- [x] 配置卡片操作统一复用内置 SVG 图标库，移除重复的来源/匹配/精确体积标签；原生窗口等待主题 CSS 连续两帧绘制后显示，避免启动闪白。
- [x] 配置、语料与历史会话的删除，以及配置编辑/导出/选中状态均统一使用内置 SVG symbol；全局操作字符图标审计无残留。
- [x] 无边框窗口最小化、最大化、拖动和关闭桥接已保留。
- [x] 浅色模式标题栏、窗口按钮与悬停色随主题切换；Sidebar/标题栏层级和布局不变。
- [x] 浏览器控制台 0 error / 0 warning；亮色标题栏、Sidebar 层级、Banner 遮罩、训练切换按钮、聊天布局和按钮底沿均完成实际渲染复验。

## 七、CPU 算子与性能

- [x] v192 混合 Q4/VQ packed MoE 算子通过集成测试；AVX-VNNI 与 AVX2 回退逐字节一致，并随包提供通用 VQ/Q4 原生反量化入口。
- [x] 随包预编译 `.pyd`、MSVC/OpenMP DLL 完整。
- [x] 随包 MSVC 19.44、Windows SDK 10.0.26100、Ninja 和 C++ 源码完整。
- [x] 预编译文件缺失时真实离线自动编译/链接测试通过，状态 `local-jit`。
- [x] 保持全部已选专家和 top-k=6；没有通过削减专家换速度。
- [x] 真实 77.3 GiB 模型 CPU 加载、prefill、连续 decode 稳定，无崩溃。
- [x] 码本常驻 CPU Tensor，转置 FP32/成对 BF16 执行副本缓存和 L2/L3 连续 tile 复用均已启用；服务器 v172 原始记录与客户端 tile 扫描随包保留。
- [x] 真实路由 prefill A/B：保留块内异步专家预取，64 token 端到端 76.276→70.958 秒且 11,008 槽位路由逐项 0 差异；负收益多行候选已撤回。
- [x] CPU packed 多行 Prefill 已以公共 `moe_topk:rows` 算子重新实现为“按专家聚合 token → 每个专家只反量化一次 → 批量 GEMM”；4096 token、TopK=6、256 专家的真实单层动态专家段为 5.025 秒（815.15 routed-token/s），全部专家保持可路由。
- [x] 公共 `moe_topk:rows` 与固定 Dense/共享专家 Q4 多行 GEMM 同时用于正常聊天长提示词和训练扫描；单 token decode 继续使用低延迟 packed GEMV。
- [x] 训练首块不再误报“加载模型”：4096-token 块内显示层进度；每个完成块原子保存真实专家计数，运行中可查看中间热力图，完成前不允许导出配置。
- [-] 至少 3 token/s：最新稳定长测中位数 2.009、最大 2.043 token/s，未达到；用户已确认停止继续追速，不得伪报通过。

## 八、本地 Python 与 EXE

- [x] `runtime/cpu/env`：Python 3.11.15 Miniconda 环境，全部运行/推理/测试/构建依赖已安装。
- [x] `runtime/miniconda`：本地环境管理器随包保留。
- [x] `PYTHONNOUSERSITE=1` 导入来源验证通过；`pip check` 无破损依赖。
- [x] 最终用户只通过 `CCCP-Launcher.exe` 启动；发行目录不再携带 BAT 入口。
- [x] PowerShell 构建脚本兼容 Windows PowerShell 5.1，无网络安装步骤。
- [x] 已补充可复现的 `archive/packaging-guides/自行打包EXE流程.md`，后续可只用随包 Miniconda 自行重建。
- [x] PyInstaller 单文件 EXE 已生成并复制到根目录。
- [x] 新增按版本号生成的干净离线封装目录脚本；排除启动器源码/测试/构建缓存，保留运行时、引擎、算子和工具链。
- [x] EXE `--no-shell` API 端到端通过。
- [x] 真正批处理双击路径通过，原生窗口与健康接口就绪。

## 九、归档、文档与验收

- [x] 旧硬编码配置、旧启发式生成器、v185–v187、top-k 实验、性能回归、旧 Python 移入 `archive`。
- [x] 模型目录未移动、未改写。
- [x] 配置库先选模型再过滤对应指纹配置；配置卡操作栏不再与文字重叠；无配置模型可选择全部专家且关闭严格路由。
- [x] 首次添加/扫描模型及模型下载完成后立即重扫模型相邻 `profiles/`；首页、配置库切换模型及“设为启动模型”均会触发配置刷新，无需重启应用。
- [x] 训练名称与描述贯通任务、详情、导出和注册；模型/配置/语料/训练四类空状态均完成加载前后虚线占位实测。
- [x] 同一训练热力图可按不同覆盖率和名称保存多份独立容量配置；配置 ID 由任务、当前专家集合和名称稳定生成，32G/64G 等版本不会互相覆盖，任务记录保存全部已生成配置。
- [x] CCCP API Key 可生成、复制、显示/隐藏、保存和关闭；`/v1` Bearer 鉴权与运行中禁止换 Key 已覆盖。
- [x] 0.9.0 最终自动化回归 `129 passed, 25 warnings`；覆盖 token/角色/4096 分块/覆盖率、多预设独立保存、首次模型扫描与下载完成自动发现相邻配置、首页模型摘要稳定性、CPU 缓存、DSV4 EOS/KV、模型能力思考档位、GPU 自动上下文、安全回归、离线发行隐私审计、首次安装器路径与状态纯净性、GitHub 发布幂等与远端审计。
- [x] CCCP I‑0～I‑5 契约全部落地：模型规格、逐专家精确字节、route-scan/运行统计、mapped 磁盘档位、配置采纳和 RAM/VRAM 驻留回执；旧“待开发”提示已移除。
- [x] 后台更新检测不阻塞启动；VisionSic 官网优先、GitHub 兜底，来源与下载页严格对应；支持稍后提醒、本版本不升级和设置页手动检查。
- [x] 中文使用手册、依赖说明、自行打包流程、归档清单、详细测试报告已完成。
- [x] 面向用户的 `docs` 严格只保留使用手册、离线依赖说明和 AMD 基础兼容说明；详细测试/开发审计仅存内部归档，不进入 EXE 或离线发行目录。
- [x] 完整依赖版本清单已完成。
- [x] 最终活动发行树 SHA-256 清单已生成；最终文件数与字节数以 `SHA256SUMS.txt` 头部为准。
- [x] 0.9.0 EXE 已在批量 Prefill 与中间热力图功能完成后重建；最终字节数与 SHA-256 见详细测试报告和发行清单。
- [x] 0.9.0 离线发行树的 CPU/CUDA/AMD 环境导入与完整 SHA-256 清单生成通过；最终文件数和字节数以清单头部为准。
- [x] 最终离线发行树为 105,123 个文件、15,246,153,905 字节（不含清单自身）；`models`、`profiles/user` 与首次运行前 `data` 均为空，用户 `docs` 仅含三份基础中文说明，v192 预编译 CPU 算子随包存在。
- [x] 最终封装目录自身启动回归通过：0.9.0、CCCP 自动探测、8 个核心页面/API 均为 HTTP 200，空模型/配置状态、多预设 UI 与覆盖率串行规划代码均生效。
- [x] GitHub `Value99/CCCP` 已包含应用提交 `7ce0681`；随后远端 Banner 提交快进至 `e5bc1fa`，仍包含最终 EXE。`v0.9.0` Release 的 9 个附件全部完成状态/字节审计；模型与 14.2 GiB 依赖树未写入 Git 历史，`VERSION` 与 `latest.json` 保持原字节不变。
- [x] `deepseek-v4-cccp-l-20260813` 95 个文件、127,205,668,103 字节完整复制；源/目标逐文件 SHA-256 全部一致。
- [x] 118 GiB 模型全量 11,008 专家加载、聊天、SSE、KV、惩罚参数、16-token 路由扫描与 UI 回归通过；详细结果见专用报告。
- [x] 32G 命名配置真实加载 1,856 个专家，动态专家 18.583 GiB、配置总驻留 26.822 GiB、运行估算 33.64 GiB；32 GiB RAM 自动页文件降级后仍完成加载和多轮生成，未减少专家。
- [x] GUI 回归覆盖流式生成/停止、Assistant 回退、仅用户消息撤回、分叉后 KV 安全冷启动、连续轮 KV 前缀复用、思考内容折叠，以及按模型声明只显示“自动/关闭/高/最大”。
- [x] GUI 训练从“开始 token 扫描”自动进入终端，实时显示任务/模型/语料/4096 块/层进度；最终完成任务保留 12,288 token 热力图与 3 份独立配置，测试任务已清理。
- [x] 最终 EXE 39,122,772 字节，SHA-256 `12C38124D1E609336F4825DE2B79CADDF9F78E4AD6E83A4A1E46BBECB72AB0D6`。
- [x] 本地配置自动发现热修 EXE 40,758,725 字节，SHA-256 `336011636A83865E88C6176E2462FD3E551B3F92253DB2637725D20A101E5688`；冻结版实测 1 个模型、3 个相邻配置全部匹配。
- [x] 最终首次安装器真实双击完成 4 分卷校验、合并、108,071 个文件解压、临时 ZIP 删除和主程序自动启动；模型/配置为空，首次设置只写入安装目录自身 `models`。

## 十、0.9.1 GLM‑5.2 回归与发布门禁

- [x] 模型识别保持通用配置驱动：读取 `cccp.json` 顶层 `model_family`，未按模型目录名或 GLM 名称增加专用推理分支。
- [x] 首次发现模型、手动选择模型和模型下载完成后自动重扫相邻 `profiles/`；首页移除容易产生竞态的“配置数量”摘要。
- [x] 发布 CLI、离线冒烟、资产生成与 GitHub 发布脚本默认从根目录 `VERSION` 读取版本；真实模型门禁通过后已统一切换为 0.9.1。
- [x] GLM 公共推理路径已接入 `moe_topk:rows` 专家聚合批处理与真实 `route_counts`；路由扫描按层优先、每层 4096-token 分块，只保留当前层 KV/专家缓存并实时上报层与块进度。
- [x] 当前源码全量自动化回归：147 passed、11 skipped；CPU/CUDA/AMD 三套离线环境 `pip check` 均无依赖冲突。新增通用 `verify_model_manifest.py`，按任意 CCCP 模型自身的 `artifact_sha256` 验收，覆盖路径越界、缺失、多余、损坏以及上游全量哈希状态。
- [x] 模型发现通用排除 `.copying`、`.partial`、`.downloading`、`.tmp` 等临时目录；真实设置扫描只显示完整 DeepSeek，不把正在续传的 GLM 暴露为损坏模型。
- [x] NAS 正式目录 `GLM-5.2-CCCP-334G-Sweet` 已于 2026-08-14 05:06:41 出现；全程未读取或处理 `.partial`。
- [x] 正式成品门禁已核对：159 个文件、358,118,503,441 字节（333.523847 GiB），`cccp.json` SHA-256 为 `08117b0c7b076ae6b502b3a96fc89f8590410c25b2e88f46e6c38fe3863c759b`；上游 `verify.json` 为全量哈希、157 个制品、19,200 专家、75 个专家层且 `errors=[]`。通用解析器识别为 `glm_moe_dsa`、78 层、256 专家/层、top-k 8、最长上下文 1,048,576。
- [x] 完整模型已由用户放到 `C:\Users\tylog\Desktop\5090\model\GLM-5.2-CCCP-334G-Sweet`；157/157 个清单制品逐文件 SHA-256 全量通过，0 缺失、0 多余、0 不匹配、0 读取错误，旧 DeepSeek 模型保留。
- [x] 修复组合 `GU + Down` 投影清单的通用 CPU 能力识别，不按模型名分支；真实 L03 专家读出为 p15/p13 两投影紧凑权重，`PackedCpuExpertPool`、内置 `cccp_cpu_kernels_v192.pyd` 和 4096-token 分层批量入口均已实际命中。
- [x] 通过 GUI 完成模型发现、32.296 GiB / 811 专家配置加载与真实 SSE 生成，实际命中公共 `cccp_cpu_kernels_v192`、75 层 Q4 多码本 MoE 执行图；流完整结束且助手气泡显示精确性能指标。
- [x] 通过 GUI 跑满 4096-token 路由扫描：75 个专家层、2,457,600 次路由观察、19,114 个命中专家；终端逐层进度、4096 分块和热力图均正常。扫描专用缓存逐层释放，运行内存不再跨层累积。
- [x] 由热力图选择 20% 目标覆盖范围并保存 `GLM-5.2 角色扮演·32G`：811 个专家、23.64% 实际逐层覆盖、13.27 GiB 动态专家、19.03 GiB 固定权重、32.296 GiB 配置总驻留；随后用该配置完成 GUI 加载和真实生成。
- [x] 修复严格路由把 Dense 层 0–2 当作专家层的问题；配置元数据精确记录专家层 3–77，严格 top-k=8 只检查这些专家层。修复组合 GU+Down Q4 单 token 解码布局分派及达到 `max_tokens` 后多算一次 decode；真实 GUI 1-token 请求由 469.7 秒降至 268.3 秒。
- [x] 上述真实模型门禁通过后已把源码、WebUI 缓存版本与更新日志统一升级至 0.9.1；官网本地成品、GitHub 主页和 `latest.json` 已按最终 EXE 哈希同步写入。
- [x] 重建并审计 0.9.1 单文件 EXE、离线封装目录、4 个压缩分卷和首次解压安装器；真实安装完成 108,074 个文件解压、临时 ZIP 删除、主程序自动启动及 0.9.1 健康检查，随后只清理验收用解压副本。
- [x] GitHub 主页已更新并发布 `v0.9.1`；9 个非模型发行附件全部为 `uploaded`，公开 API 复验附件数量、字节、版本和 EXE SHA-256 一致，仓库不含模型或启动器/推理/量化框架源码。
- [x] VisionSic 官网与 `/cccp/latest.json` 已在 0.9.2 发布后完成公网复验：首页显示 0.9.2，更新清单版本、下载地址、EXE 字节数与 SHA-256 均与 GitHub Release 一致。

## 十一、0.9.2 桌面外壳、容量降级与 CUDA 离线编译

- [x] 移除启动时、重复启动时及原生窗口失败时自动打开系统浏览器的全部路径；外壳失败会停止本地后端并显示原生错误。
- [x] GPU 容量提示按显存、主机内存、磁盘三级独立计算；RAM 卸载与磁盘卸载拥有不同状态、日志、终端文案和风险等级。
- [x] CUDA 隔离环境覆盖用户系统 `CUDA_HOME/CUDA_PATH`，动态寻找 pip cuBLAS DLL，显式定位随包 MSVC `lib.exe/link.exe`；封装门禁会生成并检查 cuBLAS 导入库。
- [x] CUDA Runtime 从当前隔离环境动态发现 `cudart64_*.dll`，不再硬编码 CUDA 12 文件名；混合 UTF-8/Windows 代码页的 nvcc/link 输出按行正确解码。
- [x] RTX 20 系 SM75、RTX 30 系 SM86、RTX 40 系 SM89、H20/H100 SM90、RTX 50 系 SM120 自动选架构测试通过；CUDA 13 `nvcc --list-gpu-code` 确认全部目标均存在。
- [x] SM120 完整融合算子在无显卡强制构建模式下通过真实 `nvcc` 编译、随包 `link.exe` 与 cuBLAS 导入库链接，173 秒完成，`AVAILABLE=True`、`ERROR=None`。
- [x] 修复关闭桌面窗口后 Python 推理后端残留：外壳同步停止模型进程并等待 uvicorn/FastAPI shutdown 完成；启动失败路径同样回收。
- [x] 冻结 0.9.2 EXE 原生窗口实测：未新增系统 Chrome/Edge/Firefox 进程；向实际 WebView 主窗口发送正常关闭事件后，应用进程退出且本地 API 端口同步释放。测试产生的本机数据和缓存已清理，交付目录恢复首次启动状态。
- [x] 新启动器进程清空上一会话 CCCP 终端与聊天指标；启动模型时自动记忆本次选择的 CPU/NVIDIA/AMD 后端。
- [x] DSV4 mapped 容量档切换到公共混合专家池：严格按当前配置白名单载入全部专家到 RAM，VRAM 只建立按真实路由替换的有界热槽；自动原地锁页后由 GPU 直接读取，实际未能锁页的来源才退回连续中转环，不减少专家、不改变 top-k。
- [x] 混合专家池容量按配置实际专家计算，不再用包含配置外专家的整层文件体积误判；物理 RAM 不足但系统虚拟内存足够时明确降级并继续，加载阶段持续输出结构化专家进度。
- [x] 主题切换立即保存到 `data/settings.json`，应用初始化从后端恢复并同步 LocalStorage；真实 WebUI 已完成“切到深色 → 刷新 → 仍为深色 → 恢复跟随系统”回归。
- [x] 修复 CUDA 混合池把“专家全量驻 RAM”误当作“专家全量驻显存”而关闭预取：显存热 arena 先于主机锁页建立，正常 RAM+VRAM 模式自动启用上一 token 跨层路由预取，且不再在每层重复提交同一预取任务。
- [x] 新增 `[cccp-cuda-audit]`：终端明确打印 Dense=`gpu-only`/`ram+cuda`、公共专家算子、RAM 专家、显存热缓存和预取开关，避免仅凭任务管理器 3D 图误判计算设备。
- [x] 修复 GPU 融合算子每次启动重复编译：缓存键包含后端、实际 GPU 架构、Torch/CUDA/ROCm、Python ABI 与源码 SHA-256，缓存位于跨版本稳定的本机应用数据目录；同一二进制直接加载并打印 `cccp-op-cache hit`，移动发行目录不会失效。
- [x] GPU 缓存真实双进程回归：空缓存 SM75 完整 NVCC/link 首次 192.863 秒；第二个独立 Python 进程直接 `cccp-op-cache hit`，1.607 秒完成且没有进入 Ninja/NVCC。
- [x] 修复 packed RAM 专家锁页的固定 64 GiB 空闲门槛和 `VRAM×1.25` 误限：自动预算只按配置专家总量与实时可用 RAM 计算并保留 2 GiB；20 GiB VRAM、36 GiB 专家、39.5 GiB 可用 RAM 会尝试完整锁页 36 GiB，不再因显卡容量只锁 25 GiB。
- [x] GLM、DSV4、Kimi 使用单一 1 GiB 默认显存物理安全线；进程物理上限不再先扣一次、专家 arena 再扣一次。专家加载按约 1% 更新，锁页阶段有独立进度；终端增加 Decode 与 H2D/cache 审计，聊天 tok/s 排除 Prefill/TTFT。
- [x] GLM 模型 API 回归确认 75 个专家层为 L3–L77，811 专家配置加载有效，不再把 Dense 层 L0–L2 纳入 top-k=8 校验。
- [x] Kimi K3 通用清单识别与训练扫描通过：93 层、L1–L92 共 896 专家/层、top-k=16；真实 teacher-forcing 扫描 16 token / 92 层，生成 11,315 专家、约 55.43 GiB 的可加载配置，未生成或改写模型权重。
- [x] Kimi CPU 严格配置推理通过：11,315 个专家全部常驻 RAM、运行期零磁盘，中文输出可读；28-token Prefill 259.079 秒、Decode 约 0.737 token/s，功能正确但明确标记为低速。
- [x] Kimi H20 单卡全显存 CUDA 通过：Dense GPU-only 约 57.5 GiB、专家 arena 55.43 GiB，4,025-token Prefill 约 96.3 token/s；中文输出、EOS、思考档位和多轮 KV 精确前缀复用均通过，单 token GEMV Prefill 回退保持禁止。
- [x] Kimi H20 单卡 16 GiB 显存硬上限通过：自动选择 RAM Dense + CUDA packed MoE，完整配置专家常驻 RAM，44.0 GiB 锁页、5.80 GiB / 1,101 个显存专家槽；无 OOM、无磁盘、无专家减少，实际输出“你好！”。因约 56 GiB Dense 位于 RAM，22-token Prefill 116.296 秒、Decode 0.577 token/s；启动器已将其明确标记为兼容降速档。
- [x] 修复 Kimi RAM-Dense CUDA 的 KDA/MLA 状态设备不一致、分层 Prefill 工作区碎片 OOM，以及 Prefill 临时 arena 未在 Decode 前恢复导致的槽位查找失败；整批传输、块级工作区复用和 arena 恢复均有回归守卫。
- [x] Kimi 最终 API 回归通过：`/v1/models` 正确声明思考支持与 low/medium/high/max；普通对话隐藏 EOS，第二轮实际命中 `exact-prefix`（baseline/lcp=38、suffix=30），思考请求把 `reasoning_content` 与正式回答分离。
- [x] DSV4 H20 单卡 16 GiB 复验通过：11,008 个配置专家全部驻留 RAM、44 GiB 锁页、1.54 GiB / 258 个显存热槽，零磁盘且未减少专家；622-token Prefill 约 135 token/s，中文回答、EOS、思考档位和多轮 LCP KV 重放正常。
- [x] 修复 Windows DSV4 冷 Prefill 的专家异步上传竞态：默认计算流用 CUDA 事件等待当前 H2D 批次后再解量化。Linux/H20 使用 `cudaMemcpyBatchAsync`；该 API 在 Windows CUDA 13/WDDM 两次复现延迟非法访问，因此 Windows 改为每层只进入一次编译扩展、在 C++ 内连续入队全部已锁页专家到同一 copy stream，不经过 CPU bounce，也不做逐专家 Python 调用。
- [x] 修复固定地址专家 arena 被通用显存监控器运行期连续裁小且无法恢复：arena 按物理上限和单一 2 GiB 安全线规划，避免 20 GiB 显卡热槽跌到 2.44 GiB 后出现 `hit=0`、每 token 数 GiB 重传；`[cccp-vram-plan]` 完整记录申请、固定占用、驱动余量、进程上限和最终容量。
- [x] 修复启动预算先扣 Dense/架构工作区、固定 arena 又按真实 CUDA 占用二次裁定的重复预留；所有架构改为 Dense/静态图加载完成后只按实际占用和统一 1 GiB 默认安全线分配专家槽，并清理父进程遗留的旧预留与测试显存上限。
- [x] 配置中的 `route_count` 已接入 CUDA 热槽：先按当前最大专家精确预留模型原生 top-k 动态槽，剩余槽按全模型真实命中次数降序常驻语料热专家；无训练热度配置才回退上一轮路由保护，不再把训练结果只用于白名单。
- [x] DSV4 使用 Prefill/Decode 阶段化 arena：Prefill 暂用单层紧凑专家容量，批量展开结束后恢复最大安全 Decode 缓存；Decode 不再固定占用整轮上一 token 的 258 槽，top-k 工作槽之外全部给热专家。显存热专家释放 RAM 重复载荷，下一次完整 Prefill 先带进度恢复紧凑主机源再安全重分区。
- [x] 新增统一驻留容量账：Dense/固定运行区、1 GiB 默认物理安全线、Prefill 单层最大工作集、码本、最大专家、top-k 最大字节底线与签名槽需求、Decode arena、热专家数和 RAM 去重体积全部在启动时输出。
- [x] r9 修复 Windows 热专家 RAM 去重调用 POSIX `malloc_trim` 的 `TypeError`，并在清理前释放批量显存槽临时视图；失败 traceback 不再阻止旧 Decode arena 回收，避免下一次 Prefill 出现 `selected=0.00GiB`。
- [x] Windows/WDDM 自动对现有 packed 专家 RAM 原地锁页；已锁页来源由 CUDA copy stream 直接提交，不再先复制到 CPU bounce ring。终端用 `[cccp-dma] mode=direct-registered cpu_bridge=disabled` 明示全量直传；锁页不完整时显示 `mode=mixed` 与准确未锁页体积。
- [x] H2D 审计拆分为纯设备 DMA、CPU 主存 staging、锁页直传字节、普通 RAM 中转字节、提交批次和拷贝次数；不再把 Python/主存整理间隙统称为 PCIe 时间。
- [x] CUDA 审计明确区分专用显存已分配/分配器保留/驱动可用与锁页专家 RAM；Windows 任务管理器把锁页 DMA RAM 计入“共享 GPU 内存”时，终端明确说明它不是显存溢出或磁盘卸载，专用显存余量保留给 Attention、KV、Prefill 和 WDDM。
- [x] 修复 DSV4 分层 Prefill 临时 arena 未在下一层布局变宽时恢复的问题；小热槽不再启用反而减速的跨层预取，终端明确显示按需批量 staging。
- [x] GLM 多轮聊天在模板边界重分词时按最长公共前缀截断并复用 KV；实际通过 `lcp-replay` 与 `exact-prefix` 两种路径。角色 EOS 不再进入回答，流式 `</think>` 支持跨分块识别并正确拆分推理/正式内容。
- [x] GLM CPU 旧 VQ 专家 Prefill 改为按专家聚合整批 token 行并调用融合列表算子，禁止逐 token/专家投影；11-token H20 CPU Prefill 由 104.229 秒降至 53.671 秒。
- [x] GLM H20 全 19,456 专家 CUDA 实际生成可读中文，Dense 约 24.9 GiB、专家约 192.1 GiB 驻留 RAM、12.07 GiB / 1,222 个显存热槽；811 专家窄配置在 CPU/CUDA 均出现重复输出，证明该配置覆盖不足，不能作为通用质量预设。
- [x] RTX 3090 现场回归暴露旧 `profile-hot` 独占槽、Prefill `expert chunk=1` 和单请求百 GiB 级重传；阶段化 arena、统一安全线及原子槽保护已修复容量/崩溃问题。最终运行期采用无永久保护的严格 LRU，按真实训练热度预热、全部槽位可随 KV 增长收缩和末位淘汰。
- [x] Decode 终端审计现在直接记录 `cuda.packed_moe_topk_fused`、融合核提交数、CUDA Graph 提交数和诊断参考路径次数；0.9.2 发布后源码完整自动化回归为 271 passed、11 skipped、31 warnings，跳过项仍为本机缺少对应 GPU 的硬件执行测试。
- [x] 重建包含当前 Kimi/DSV4/GLM、稳定 GPU 算子缓存与 GUI 修复的 0.9.2 单文件 EXE及完整离线测试目录；CPU/CUDA/AMD、cuBLAS、CUDA Runtime、内容边界、关键引擎文件同源哈希和冻结 API 冒烟全部通过。
- [x] 修复发行清单生成后，冻结冒烟的依赖探测在目录中新增 `__pycache__` 的流程漏洞；清理 2,962 个可再生缓存后重建 105,125 文件清单，复跑冒烟保持 0 个新增缓存目录。
- [x] 新增发行清单只读验证器并接入 `release_cli verify` 与压缩资产构建前硬门禁；正式离线目录 105,125 个文件、15,248,020,215 字节已在冻结 API 冒烟后逐文件复算 SHA-256，0 缺失、0 多余、0 不匹配。
- [x] 已生成 0.9.2 ZIP64 四分卷、parts 清单、首次解压安装器、独立 EXE 与全量 SHA-256；封装目录冒烟、安装/发布审计及分卷顺序合并校验全部通过，合并归档 SHA-256 为 `322da0573dccdc7132cbbc9826ba4f9296ecb9dc58aeddda8f979ffa27e0df12`。
- [x] H20 当前代码门禁已覆盖 Kimi K3、DSV4 与 GLM：自动容量规划、批量 CUDA 专家算子、CPU 分组 Prefill、RAM 驻留、锁页、H2D、EOS、思考解析、KV 复用、真实生成与性能数据均已记录。
- [x] 修复 GPU 第二轮短续写在 Windows/大模型上可能由静态图旧状态产生非有限路由、继而选中配置外专家并报 `packed RAM expert missing`：规范 KV 分支重建期间绕开静态 Attention/FFN 图但保留融合 CUDA 算子；DSV4/GLM 融合路由从内核层严格排除掩码外专家。
- [x] H20 新算子实测：全 NaN 路由只选择掩码允许的专家 50–55，正常有限输入与参考 Top-K 完全一致且权重最大误差为 0；静态图开启的两轮真实对话第二轮 `baseline/lcp=45`、`replay=0`、仅处理 6 个新 token，Prefill 1.16 秒、Decode 14.65 token/s，无配置外专家或非法显存访问。
- [x] r13 本地自动化回归为 265 passed、11 skipped；Windows 覆盖包已审计为 19 个推理运行文件，不含模型与量化框架。
- [x] r14 不改 top-p/温度/惩罚采样算法；在进入采样前只检查一次 DSV4 Prompt 原始 logits，非有限值直接报告上游模型状态错误，不做隐藏完整重建、不伪造 token，也不以采样补丁掩盖问题。
- [x] Windows 与 Linux/H20 统一使用已验证的短后缀 `incremental-fused-decode`；规范 KV 分支期间绕开可能携带旧地址的静态 Attention/FFN 包，但继续使用融合 CUDA 算子，不保留 Windows 专用逐层慢路径。
- [x] 终端新增真实 CUDA 分阶段计时：Attention HC Norm、Attention、FFN HC Norm、MoE、FFN Post，以及 MoE 内部 Route、Shared、Routed、Merge、其他主机/同步开销和最慢三层；每个请求只探测一个真实 Decode token，普通执行器与算子不替换。
- [x] H20 两轮真实直传回归通过：12 GiB 专家 RAM 全量原地锁页，`direct=17.985GiB`、`staged=0`、`host_stage=0`；第二轮 `baseline/lcp=45`、`replay=0`、只处理新增 6 token，Prefill 约 759 ms、Decode 16.249 token/s。当前本地自动化回归为 269 passed、11 skipped。
- [x] RTX 3090 现场直传确认 `direct=112.550GiB`、`staged=0`、`host_stage=0`，排除 CPU bounce bridge；但旧实现的 96 token 曾发生 11,049 次专家未命中、上传 112.55 GiB，Decode 仅 1.447 token/s。根因收敛到每层小拷贝提交和显存槽抖动；最终 Windows/WDDM 使用一次 Python→扩展提交、扩展内部同一 copy stream 紧凑循环 `cudaMemcpyAsync`，Linux/TCC 保留原生 `cudaMemcpyBatchAsync`。SM86 CUDA 13 扩展编译/链接和跨进程缓存命中均通过，并确认导出批量 H2D、融合 Decode MoE、分组 Prefill MoE与投影解量化入口。
- [x] GitHub `v0.9.2` 已正式发布 9 个非模型附件；公网 API 复验全部为 `uploaded` 且字节数与本地一致，公网重新下载独立 EXE 的 SHA-256 为 `696A72F3200BEAA023C53164F236BD2185DC30486D31F2288500681C0093EC10`。公开仓库仅 29 个说明/图片/版本元数据文件，未发现启动器、推理引擎、量化框架源码或模型权重。
- [x] 清除官网三语言基础字典、官网实验链接、GitHub 三语言主页和质量图表中的旧品牌；统一字段为 `cccpIntro`、链接为 `Value99/CCCP`，并新增公开渠道旧品牌零命中自动化门禁。GitHub 公网提交 `fefdf31` 复验 29 个公开文件旧品牌命中为 0；VisionSic 本地待部署 HTML 命中为 0。

## 未完成硬指标

未完成硬指标：真实 CPU 性能仍为约 2.009 token/s，未达到 3 token/s。AMD ROCm/HIP 环境与算子自动编译路径已经实现，但当前构建机无 AMD GPU，硬件端数值与 token/s 需在官方支持的 AMD 目标机完成最终验收，不能把环境导入通过等同于硬件性能已验证。基础兼容边界见 `docs/AMD核显兼容性说明.md`，内部实测记录位于 `archive/docs-internal-20260813`。

## 十二、Qwen3.5 Dense 通用适配与下一版门禁

- [x] 在独立开发分支 `codex/qwen35-cpu-opt-v093` 完成当前最优路线，保留并复验 DSV4、Kimi、GLM 修复。
- [x] 核对 `qwen3.5-27b-cccp-15g-sweet` 清单：64 层、Dense VQ、无动态专家；训练/路由扫描必须显示“不适用”，不得伪造专家配置。
- [x] 修复 AMD 全显存驻留却被父进程旧环境关闭 TP1 TokenGraph 的性能漏洞；GUI 启动隔离旧变量，HIP 全驻留 TP1 把父图设为运行时必需路径，CPU/NVIDIA 分派不变。
- [x] 生成 Windows 累积测试覆盖包 r26：冻结 EXE 与 95 个推理文件同源；本地 289 passed、11 skipped，冻结健康接口实测通过。AMD 验收要求 `decode_graph>0`，图构建失败必须显式报错而不能静默回退逐层 eager。
- [x] r28 将 HIP 全显存 TokenGraph 门禁扩展到阶段计时状态，图未回放时无条件拒绝 eager 慢路径；执行器日志按实际 HIP 后端输出。完整自动化 303 passed、11 skipped、0 failed。
- [x] CPU 原生扩展版本同步为 v193，ABI 匹配的预编译 `.pyd` 已进入源码与 r28；关闭自动编译的离线导入自检通过，覆盖目录 100/100 个推理文件 SHA-256 与当前源码一致。
- [x] 建立配置驱动的 Dense VQ 能力注册、清单张量加载和模型执行入口；公共存储/算子不按目录名判断，Qwen 仅保留独立架构状态机适配器。
- [x] Qwen3.5 单卡 H20 完整实模、有限 logits、长 Decode 与可读输出通过：Prefill 527.095 token/s；MTP Top-3 接受 49/57 个草稿 token，有效 Decode 93.209 token/s，64-token 完整请求 82.392 token/s。
- [x] GUI 模型库、首页、聊天与训练页按模型能力自动显示；Dense 直接全量加载并隐藏专家预算、热力图和配置训练。真实冻结 GUI/CPU 启动、终端进度、SSE 聊天与停止回归通过。
- [-] CPU 功能和数值通过；修复 Q4 执行映像复用旧 NUMA 页后，H20 双路 Xeon 64-token Decode 由约 7.05 提升到 9.77 token/s（+38.6%），仍未达到 >30 设想值，不写成达标。
- [x] DeepSeek-V4 单卡、Kimi K3 TP4、GLM-5.2 TP2 实模重新回归；Kimi 10/15 行组合投影进入 packed grouped 融合执行器，没有回退逐 token/专家 GEMV。
- [x] Kimi TP4 固定图生命周期门禁通过：82,432 专家 / 414.23 GiB 全显存、运行期 H2D=0；父图完成后最终捕获全部 69 层 KDA，停用不稳定的整层 Decode 父计划但保留融合子图；30-token Prefill 7.114 s，93 层 Decode 输出严格为 `2`，无非法显存访问。
- [x] 完整自动化 322 passed、11 skipped、0 failed；0.9.3 版本、官网草稿、中文手册、更新清单、更新日志和独立测试报告已完成。
- [x] 已构建并审计 0.9.3 EXE（40,780,911 字节，SHA-256 `583D1E627B6F2479AE60BF2E5260982B18D94924F1BDD619DE4CE9FCC446E5E1`）、105,363 文件 / 15,381,473,644 字节离线目录、4 个 GitHub 分卷与双击安装器；模型、预设、测试报告和量化/训练框架均未进入发行包，冻结 API 冒烟、逐文件清单与分卷重组 SHA-256 复核通过。
- [x] 双击安装器真实回归退出码为 0，完成分卷校验、6,505,133,446 字节归档重组、105,000+ 文件解压、临时 ZIP 删除与应用自动启动；验收后只删除可由分卷重建的测试安装副本。
- [x] 公共仓库 `main` 已快进到 `8751c95`，GitHub `v0.9.3` 已发布 9 个非模型资产；公开 API 复验全部为 `uploaded`，远端文件大小和 SHA-256 digest 与本地一致。
- [-] 本地 `官网/index.html` 与三份 `latest.json` 已更新为 0.9.3，但 VisionSic IIS 公网仍返回 0.9.2；本机没有该 IIS 的 FTP/WebDeploy/SSH/控制面板发布会话，不能伪报官网已上线。GitHub Raw 更新清单已为 0.9.3。
- [-] AMD 当前源码要求 `hip.tp1-token-graph` 且 `decode_graph>0`；用户之前 2.11 token/s 日志来自旧 r27 混合目录。本轮无 AMD 真机，只把离线依赖、编译路由与自动化门禁记为通过，不声明 AMD 实模性能。
- [x] 0.9.4 CPU 原生扩展升级为 v195；Windows 预编译 `.pyd` 已生成，旧 v194 已移出发行源码树；完整自动化 324 passed、11 skipped、0 failed。
- [x] 0.9.4 成品封装通过冻结 EXE 冒烟、内容审计与 105,363 文件逐项校验；离线目录 15,381,475,504 字节，ZIP64 归档 6,505,134,338 字节并拆为 4 个 GitHub 分卷，归档 SHA-256 为 `00b68247a015246622d21453ac2c500305df75a227bf26dd56688642b555b42b`。
- [x] 公开仓库 `main` 已推送到 `f6cce41`，GitHub `v0.9.4` 已发布 9 个非模型附件；公网 API 复验全部为 `uploaded`，远端字节数和 SHA-256 digest 与本地一致，GitHub Raw 更新清单为 0.9.4。
- [-] VisionSic 本地官网成品已更新到 0.9.4，但公网 IIS 仍返回 0.9.2；当前机器没有该 IIS 的 FTP/WebDeploy/SSH/控制面板发布凭据，不能伪报官网已经同步。

## 十三、DSV4 长上下文唯一 FlashMLA 路线

- [x] NVIDIA CUDA 的 DSV4 上下文进入 `topk512` 后强制使用 FP8 Indexer、固定工作区 Top-K、FlashMLA sparse SplitKV 与融合逆 RoPE；删除 BF16 Indexer、`torch.topk` 和 BF16 全量 Attention 静默回退。
- [x] 删除 `CCCP_SPARSE_SPLITKV` 运行开关；FlashMLA 缺失或算子不匹配时在初始化/图构建阶段明确失败，不生成 `sparse=bf16-exact` 成功日志。
- [x] 修复非 16 对齐上下文的 FP8 Indexer 执行宽度；1092 个逻辑候选使用 1104 宽矩阵，H20 分配与实模运行通过。
- [x] H20 4096-token 实模严格路径通过：日志仅为 `flashmla-model1-fp8-splitkv`，输出有限，稳态 Decode 32.36 token/s。
- [-] 当前 Windows CUDA 环境未内置 FlashMLA；DSV4 的 2052 token 以上 NVIDIA 长上下文会明确失败而不回退慢速 BF16。0.9.3 按 2051 token 以内的已验证范围发布，长上下文不作支持承诺。

## 2026-08-18 H20 Qwen3.8 GEMV 优化(进行中)

- [x] H20 引擎双向同步:H20 最新 Qwen GPU 路径(image 计划/graph/MTP/ops 子包)合入本地 main,9 个 commit,326 passed/11 skipped;远端 H20 工作副本反超本地的历史分叉已消除。
- [x] int4(INT4-G64) 15.9GiB 显存档位打通:补齐 compile_int4_g64/mma_packed_m1/FP8 转码算子闭包,单卡 H20 裸 Decode 46.1 token/s(fp8 全显存 28.1GiB 档为 65.0)。
- [x] GEMV kernel 家族实测(端到端):v1 vector4 46.1 > v4s 分段 44.2 > vector8 38.5 > v2 split-K 30.1 token/s;v1(每 warp 一行+shared x+stride-2 无冲突)为该设计家族上限,带宽 ~650GB/s(L2 微基准虚高至 610-772)。三实验 kernel 以 env 开关保留(CCCP_INT4_GEMV_V2/V8/V4S),默认 v1。
- [-] 100 token/s 硬指标未达:差距 2.2×。账本:int4 GEMV 端到端 ~9-12ms/token(kernel 级真实带宽 ~650GB/s,需 Marlin 式 TensorCore int4 重构至 2TB/s 级);公共路径(GQA 16 层/delta 0.5ms/lm_head 0.67ms/graph 间隙)~12ms 待分段优化。CUPTI/nsys 在该 kernel 组合下崩溃或不可写,profile 靠消融法+原语微基准闭环。
- [x] 全部新算子无 __CUDA_ARCH__ 分支、无 wgmma/cluster/TMA、仅 LDG+FMA+warp shuffle,SM70+ 通用;多架构预编译门禁(0.9.5)将在发布时兜底。
- [x] Marlin 式原型判定(2026-08-18):mma.sync m16n8k16+寄存器 int4 dequant 数值验证通过(p99 2.98%);不 repack 的宽读上限实测 748GB/s(shfl 重分布),证明 2TB/s 必须走 compile 期权重重排(int4_g64→marlin tile,复用 compile_int4_g64 架构);repack 后按论文可达 ~3.5TB/s(H20 DRAM 90%),15G 档 100 token/s 的 GEMV 份额(≤5ms/token)判定可行。原型脚本 scripts/bench_marlin_int4_proto.py(含冲激诊断)。下一步:实现 repack kernel + ldmatrix 版 GEMV,接入 dense_vq(CCCP_INT4_GEMV_MARLIN=1)。
- [x] Marlin repack 全链路(2026-08-18 第二轮):repack kernel(int4_g64→超步 tile,每 lane 一 uint32 全合并读)+ mma.sync GEMV 双步/超步,数值验证通过(p99 2.98%);单 kernel 带宽 472GB/s。四变体终局:repack+marlin 472 > shfl 单步 748(数值错误期)/双步 431 > v1 ~650(端到端推算)。结论:batch=1 裸 mma.sync 不可达 2TB/s(TC 利用率 1/16);完整 Marlin(异步双缓冲+ldmatrix+stream-k+batch>1 verify 场景)为后续多周工程。端到端 int4 档维持 v1 46.1 tok/s @15.9GiB;100 token/s 需 GEMV(完整 Marlin)+GQA 公共路径(~12ms/token)双线突破。
- [x] int4+MTP 合跑与 verify 坍塌修复(2026-08-18 第三轮):int4 档 batch>1 原走全量 int4→FP16 反量化+GEMM(权重字节 4× 流过 HBM/每次 verify),MTP 实测坍塌 0.63 token/s;改为 2..16 批逐 token 融合 GEMV 循环(权重只读 int4 原始字节)后 7.8 token/s(12×,commit 326 绿)。量化结论:MTP 每 token 带宽成本=(1+draft/accept)×,int4 GEMV 650GB/s 带宽瓶颈下 MTP 综合低于裸 decode(46.1),**batch>1 摊薄路线依赖融合 int4 GEMM(即完整 Marlin 工程件)使批量成本逼近 1×**;fp8 档 MTP 97.7 的优势来自 fp8 GEMV 3.1ms/token 的低单位成本。100 token/s 两杠杆不变:GEMV 带宽(650→1500GB/s,需 cp.async+ldmatrix+stream-K 完整件)与公共路径(12.3ms/token 的 graph 间隙/GQA 分段)。
- [x] v16/v17 探索(2026-08-18 第四轮):v16(16B/lane 宽读+x 走 L1)实测 110GB/s 死路——散读 L1 泛滥,v2 的 shared bank 冲突教训需用无冲突 shared 而非绕开 shared。v17(marlin repack tile 布局+8×FMA+无冲突 shared-x+4-lane 组归约):单 kernel 731GB/s(5120x17408)/1000GB/s(lm_head 248320x5120),数值 p99_rel=0.0000(fp32 累积),已接入 CCCP_INT4_GEMV_V17=1(commit 326 绿)。端到端 int4 46.1→46.4(+0.6%)——证实微基准(L2 常驻)与端到端(DRAM 冷读)差距吞掉 kernel 级增益,decode GEMV 端到端瓶颈为 DRAM 冷读带宽而非 kernel 指令效率;cp.async 双缓冲(权重预取掩盖冷读延迟)是下一个未做的明确动作,1500GB/s 判定仍开放。
- [x] cp.async 双缓冲判定(2026-08-18 第五轮):v18 数值 p99=0,冷读流(60 矩阵,修正 L2 虚高)实测 688GB/s,低于 v17 的 736——延迟掩盖组件无效,因每指令 128B 请求粒度未变。关键校准:v17 冷读 736≈L2 微基准 731,证明 v17 系列一直在 DRAM 带宽上运行,带宽上限由访存模式决定(128B 跳跃流,DRAM 效率 ~18%)。下一步蓝图 v19:repack 第二层 k-slice 连续化(每 8 行的 2048 列段=8KB 连续),warp 顺序宽流读,目标 1500GB/s。
- [x] v19/v20 判定(2026-08-18 第六轮):ILP-2 配对(650)与行-每-lane(261,布局错配)两路证伪;D2D 拷贝对照 3850GB/s 定音——736 上限归因为每 lane 4B 加载指令宽度(LSU 4× 指令)。最终杠杆:superstep-interleave repack(word 序 [sg][j][i][4 超步连续],lane 一条 uint4 覆盖 4 超步同 (j,i)),v17 kernel 微改即可吃 16B/lane;预计 736→1300-1500。此为下轮首件。
- [x] v21 superstep-interleave(2026-08-18 第七轮):repack 第二层 [sg][j][i][u] word 序,lane 一条 uint4 覆盖 4 超步(16B/lane);冷读 892GB/s(v17 736,+21%),数值 p99=0。距 1500 仍差 1.7×:剩余指令开销候选=scale __ldg(64 次/超步/warp,shared 缓存尝试因脚本误插中止)与 32 累加器 FMA 链。端到端接入预估仅 +2%(公共路径稀释),判定:kernel 单点优化接近收益递减区,100 token/s 需要 kernel(892→1200+)+公共路径(12.3ms)双线且公共路径权重更大。
- [x] scale 缓存证伪(2026-08-18 第八轮):v21+shared scale 860 vs 无缓存 892——scales 1.6MB 本 L1 常驻;kernel 终版判定 v21=892GB/s(+21%),接入三件套预估 +2% 留待。双线中公共路径(GQA/graph,~12.3ms/token)成为唯一大杠杆;dmon 已确认可用,下轮 GPU util 采样定位串行空隙 vs 计算密。
- [x] v21 接入+终局账本修正(2026-08-19 第九轮):端到端 46.4→48.6 tok/s(+4.8%,CCCP_INT4_GEMV_V21=1,326 绿)。dmon 采样 decode 期 SM=13%/MEM=2%,graph 确认正常(1 launch/token)——**修正旧账本**:所谓公共路径 12.3ms 实不存在,delta/lm/GQA 已融合进 graph 仅 ~2ms;20.6ms/token ≈ 430 次 GEMV kernel 串行总和(0.055ms×430 自洽)。通往 100 的最终数学:GEMV 需 892→1850GB/s(串行消除或 D2D 级效率),D2D 3855 证明 DRAM 物理可行但 GEMV 的解包+FMA 指令流是剩余约束,需 nsys 级流水分析定位,为后续工程。
- [-] SASS 级停顿分析被权限封死(2026-08-19 第十轮):ncu 连通但 ERR_NVGPUCTRPERM(GPU 性能计数器需管理员权限+驱动 NVreg_RestrictProfilingToAdminUsers=0,tyh20 无 sudo)——892→1850GB/s 的指令级瓶颈(LSU/FMA/调度)无法定量定位。fallback:occupancy 手算 v21=100% warp 占用(16 blocks/SM,4 warp/block,64 warps 满额,shared 8KB 非限制)——非占用瓶颈;ILP 已深(4×uint4 在飞+32 独立 acc)。跨矩阵分组融合(430 串行→按层批量)是结构正确的下一步,但需 dense_vq 调度层重写,本轮预算内无法完成正确实现与验证。终态:int4 15.9GiB 档 48.6 token/s;通往 100 的两路径(GEMV 1850GB/s 或分组融合)均已量化但受权限/工程量约束。
- [x] 分组融合核查(2026-08-19 第十一轮):DenseVQLinearGroup.project 已实现同输入缓存拼接(组首成员触发 _forward_combined 一次 GEMV,其余成员 narrow 切片;int4 组 combined_payload/gpu_scales 编译期已拼)——**verifier 指定的跨矩阵融合 kernel 已存在于引擎**,无需重写。账本二次修正:每 token launch ≈ 组数 175+lm_head 等 ~180 次(非 430),20.6ms/token ≈ 180×~0.11ms,与 v21 单 kernel 892GB/s 上限完全自洽。端到端瓶颈最终收敛为单一变量:组 GEMV kernel 带宽(892/3855=DRAM 效率 23%),其指令级定性需 GPU 计数器权限(封死)。int4 15.9GiB 档终态 48.6 token/s;100 需 kernel 带宽翻倍至 ~1850GB/s,属权限解锁后的后续工程。
- [x] SASS 静态分析(免权限,2026-08-19 第十二轮):cuobjdump 反汇编 v21 kernel 循环体(每 lane 32B 权重)~1280 条 SASS:浮点类 514(I2FP 160+FMUL 160+FFMA 160+FADD 34)、整数位运算 603(VIADD/SHF/LOP3/IMAD)、访存 163(LDG 54+LDS 80+STS 29)。**瓶颈定论:解包开销**——每 nibble 一条 I2FP(int→float)+一条 FMUL(×scale),scale 乘未融合;且每超步 sc 与 lane i 无关(32 列超步不跨 64 列组),可把 sc 预乘进 shared-x 装载,消 160 FMUL(~12% 指令)+SHF/LOP3 部分归并,预估 892→1000-1100GB/s。要达 1850 仍需消除 I2FP(fp16 LUT 化字节→half2)——下一迭代两件套。管理员请求:解锁 GPU 计数器(驱动 NVreg_RestrictProfilingToAdminUsers=0+重启或授 sudo)后 ncu 动态验证。
- [-] v22 LUT 证伪(2026-08-19 第十三轮):行 LUT(n-8 half)+逐超步 sc hmul2+HFMA2,数值 p99=1.1%,冷读 696GB/s<v21 892——LDS/half2 打包开销>I2FP/FMUL 节省。SASS 静态两步假设(sc 预乘+LUT)均实测否证:静态计数≠动态瓶颈,v21 的 I2FP/FMUL 已被调度重叠掩盖。终局唯一前进路径=ncu 动态计数器(管理员解锁 NVreg_RestrictProfilingToAdminUsers=0,请求已提待执行)。int4 15.9GiB 档定格 48.6 tok/s(v21)。
- [x] 免权限动态终审(2026-08-19 第十四轮):clock64 自计时 probe(绕过 ncu 权限封锁)——load 19/compute 4211 cycles/128 列/lane,加载占比 0%:访存完全被掩盖,v21 892GB/s 瓶颈=计算段指令流(~33 cycles/列)。完整归因链条闭合(静态 SASS→LUT 证伪→动态自计时);下一处方=纯 HFMA2 数学或 warp 双行把计算段减半,预估 892→1500+。int4 15.9GiB 档终态 48.6 tok/s(v21)。
- [-] v24 half2 证伪(2026-08-19 第十五轮):位技巧 912(NaN,位模式非线性)/安全 cvt 825(p99=1.1%)均≤v21 892——SM90 FP16 管线=FP32 吞吐(HW 事实),half2 处方封死。19 个变体判定链闭合:终版 v21(int4 15.9GiB 档 48.6 tok/s);未试处方=warp 双行(分摊 scale/循环开销);100 token/s 需 892→1850,在 SM90 FP32 管线约束下须计算指令数结构性减半(如 dp4a int8 路线或 mma FP16 非 wmma 变体),为后续工程。
- [-] v25 dp4a 证伪(2026-08-19 第十六轮):通道列错位(权重[c,c+1,c+8,c+9] vs x[c..c+3])+472GB/s——修复需 x 侧 PRMT 重排吃掉节省,SM90 int 管线无净优势。**20 变体终局**:v21=892GB/s/48.6 tok/s 为 int4_g64 格式下 kernel 优化终点;五条更快管线处方(half2/dp4a/mma/LUT/cp.async)全被 SM90 硬件特性证伪。终论:100 token/s 需格式级变更(int8 权重重新量化或 fp8 混合档 15.9→~17GiB),超出 kernel 迭代范畴,建议作为独立决策提交。
- [x] 格式级可达性终局推演(2026-08-19 第十七轮):全部用 H20 实测数据——int4 全档理论 69.5 tok/s(实测 48.6,差=公共开销)、fp8 全档 32、fp8×30% 混合(15.8GiB)51.4、int8 权重 25GiB 超 15G 物理预算;达 100 需 int4 kernel 1283GB/s(SM90 实测封顶 892)。**终局定理:H20 单卡 15G 显存 100 tok/s 在现有格式空间数学不可达**,需 (a)GPU 世代升级 (b)模型瘦身<9GiB (c)放宽显存/多卡。goal 全证据链闭环:20 kernel 变体+clock64 终审+格式推演,42+ commits,326 pytest 绿。
- [x] 区间工程处方(2026-08-19 第十八轮终版):48.6 vs 69.5 差距构成=小矩阵低效(k<=5120 的 GEMV 584 vs 大矩阵 892,加权有效 753GB/s)。归一处方=小矩阵 k-slice 2048→512(块数x4 改善 ramp/占用,预估 584→750+)+组间 reduce 合并(180→~90 launch);完全归一可达 ~59 tok/s(GEMV 14.4ms+公共 6.2ms),69.5 需另消公共开销(scale/reduce/图内间隙)。**决策面板已交用户**:a 新 GPU/b 模型瘦身/c 放宽显存|或区间工程(48.6→59~69.5)。goal 证据链 18 轮完整闭环。
- [x] MTP 路径完整复盘(2026-08-19 第十九轮,用户点醒后的实证闭环):
  1) v1+v1 双关(V21=0,V21B=0):accepted=5/12,7.79 tok/s——精确复现第三轮,排除环境漂移;
  2) v21 参与任一侧(draft 或 verify):accepted=0/502——v21 的 fp32 累积顺序差(max_abs 2e-5)在 fast-top3 严格 greedy 比较下系统性翻转近边界 token;裸 decode 正常但 MTP 择优不可信,v21 与 MTP 不兼容(实证);
  3) v21b batched(B<=5 单权重流):合成数值 OK+verify 1.28x/token——但依赖 v21 布局故 acceptance 同崩;
  4) v1 MTP 每轮 342ms=graph 内 kernel 总和(verify 430 矩阵×5 token + draft 图),非 Python 开销。
  MTP 终评:通往 100 需 v1-batched kernel+draft 图瘦身,乐观 40-55 tok/s(计算段×B 无免费午餐,4211 cycles/token 实测封顶);MTP 不推翻终局定理,反而再证计算段约束。
- [x] v1b 修复+MTP acceptance 恢复(2026-08-18 第二十轮):
  1) **两轮编译僵局根因**:文件头 `#define ROWS_PER_BLOCK 32`(第 102 行,首代 kernel 遗留)把 v1b 模板参数宏展开成 `template <int 32>`——报错 "expected a ',' or '>'" 但指不出真因;改名 V1B_RPB 修复。教训:单文件 bench 无该宏故永远编译通过,集成错误只在引擎上下文显形。
  2) **两个数值集成 bug**(0/502 双因):(a) fusedext 包装器把第 5 参当 groups 数,引擎传的是 group_size=64——cols=5120 只算 64/80 组,输出整体错误;(b) v1b 复刻的是 scalar kernel 求和序,而引擎 per-token 实走 vector4 kernel(groups%4==0 时,lane→group 映射 lane>>3、width-8 scale shuffle、部分和 warp 树归约)。重写为 vector4 序+k-slice 分块后,**对引擎真实 per-token 路径 8 形状 bit 一致 max_abs=0.000e+00**(scripts/ab_v1b_vs_v1.py,含 6144x16384 特例)。
  3) **MTP 端到端恢复**:int4 档(CCCP_DENSE_VQ_GPU_IMAGE=int4,15.65GiB)speculative draft-tokens 4——turn1 accepted=5/12(精确基线),decode 7.56 tok/s≈v1+v1 对照 7.79;turn2 2/12。
  4) **v1b 速度平台**:branch-free b 循环+float2/float4 共享读+预取三步 77-111→125-194GB/s(后两步零增益);大矩阵与 5×vector4 循环打平(1.04-1.08x),2048 方阵 2.7x。 plateau 归因:每 128 权重字节 ~50 指令流(40 FFMA 为 5-token 数学下限)×寄存器/占用张力,bit 一致约束封死 packed-math 减半路径。
  5) **MTP 上限定量**:plain decode 46.16 tok/s @15.90GiB(本轮回归,finite_logits ✓);MTP 轮 309ms=verify(~120ms,5×T1)+draft/attn/graph(~185ms)——verify 仅占 40%,**即使 v1b 完美(22ms 单扫)MTP 也只能到 ~15.5 tok/s,结构性上限≈plain decode 档位**;第十九轮"乐观 40-55"被轮时间构成否证。MTP+int4 定格:acceptance 恢复但速度无增益,v1b 价值=bit 一致批处理基座(未来完整 Marlin verify 可替换其速度侧)。
  6) 脚本资产:scripts/ab_v1b_vs_v1.py(bit 门禁)、scripts/bench_v1b_vs_v1_speed.py、scripts/check_balance.py(宏冲突类错误的一级诊断)。
- [x] 小矩阵 k-slice 归一实测否证(2026-08-18 第二十一轮):
  1) **实施**:v21 kernel 模板化 SLICE(512/2048),auto=cols<=5120 用 512(env CCCP_INT4_V21_KSLICE 可强制),块数 x4。
  2) **内核级(payload 轮转×6 冷测,scripts/bench_v21_kslice.py)**:o 类 5120x5120 567→653GB/s(+15%);qkv/gu 类(15360/17408 x 5120)839/862→857/870(+2%);down(5120x17408)880 与 lm_head 1132 不变。**Round-18 的"k<=5120 整类 584"前提不复现**——慢类实为小 rows×小 cols 的 o_proj 类,仅占层权重 ~9%(13.1/141.6MB)。
  3) **端到端**:auto 48.40 vs 强制 2048 48.62 vs 基线 48.6——平坦(±0.5% 噪声内)。处方"48.6→~59"被否证:小 k slice 不是有效差距构成,加权内核带宽 822→837GB/s(+1.8%)被非 GEMV 时间稀释。
  4) **reduce 合并上限定量**:~180 launch×~2μs 图内延迟≈0.36ms/20.6ms≈+0.8 tok/s——噪声级,按节约指令不实施。
  5) **显存账本修正**:V21=1 的 repack 双缓存(payload+_v21_payload)实测 20.06GiB(V21=0 为 15.90)——历史"48.6@15.9GiB"系两配置数字错误合并;**≤16GiB 档真实速度=46.16 tok/s(V21=0 默认),48.6 档需 20.1GiB**。
  6) 终态维持:100 token/s 不可达定理(第十七轮)不变;区间工程(48.6→59)经实测排除;突破需决策面板 a(新 GPU)/b(模型瘦身)/c(放宽显存或多卡)。
- [x] MTP 三连修复+4.1x(2026-08-19 第二十二轮,用户指令"优化 mtp"):
  1) **轮分解插桩**(CCCP_MTP_PROFILE=1,相位末同步修正归因):verify 279ms/轮(94%)为唯一大头;accept 126ms/轮为 CPU 墙钟假象(等待 GPU 排空)。
  2) **torch profiler+chrome trace 三级定位**:GPU 时间 64% 在 index_elementwise(9411 次)+配对 elementwise——调用栈指向 kernels.py dequant_int4 的 256 项 LUT 逐字节 gather;调用方=**DenseVQLinearGroup._forward_combined 的 batch!=1 分支走 Int4Weight 整矩阵反量化+GEMM**——每次 verify 把 13.5GiB int4 物化成 ~27GiB fp16 读写。修为 v1b 批流扫:verify 279→88.3ms/轮。
  3) **accepted_prefix 批量化**(一次 topk/一次 isfinite/一次 .item()):126→4.8ms/轮,判定语义逐位不变。
  4) **v21/v21b wrapper groups-64 语义 bug**(与第二十轮 v1b 同类):第 5 参是 group_size=64 被当 groups 数直传,C++ 用 scales+row*groups 定行基址→**每个矩阵从第 1 行起 scale 全错位**——输出有限但内容乱码(finite 检查测不出)。**第十九轮"v21 与 MTP 数值不兼容"系误诊,真凶即此**;(2048,2048) 的 NaN 同源。修复+v21b k-slice 模板对齐后:v21b 对 v21 全形状(含 2048x2048)bit 一致 0.000e+00。
  5) **实测终态**:MTP+int4+15.65GiB:8.05→**33.28 tok/s**(4.1x;v21b verify 路径 V21=1+VERIFY=v21b),acceptance 精确保持(聊天 5/12,数字题 39/64);verify 280→78ms/轮。plain decode 两档内容均已验证正确:46.2(v1 默认,15.9GiB)/48.6(v21,20.1GiB——**修复前该档输出为乱码,速度数字不变但正确性自本轮才成立**)。
  6) 脚本:bench_mtp_long.py(长输出 MTP 基准,CCCP_TORCH_PROFILE 支持)、ab_v21b_vs_v21.py(bit 门禁)。
